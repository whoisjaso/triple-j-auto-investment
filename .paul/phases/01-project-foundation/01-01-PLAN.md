---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - .env.local.example
  - .gitignore
autonomous: true
---

<objective>
## Goal
Initialize the Next.js 15 project with App Router, TypeScript, Tailwind CSS, and Supabase client. Establish folder structure, design tokens, and a running base layout.

## Purpose
This is the foundation everything else builds on. A clean Next.js project with the right structure, theme, and tooling means every subsequent phase starts from a solid base. Gets `next dev` running with the Triple J brand identity from the first line of code.

## Output
- Running Next.js 15 project with `npm run dev`
- Tailwind CSS configured with Triple J design tokens (colors, fonts)
- Supabase client configured for both client and server components
- Folder structure ready for all subsequent phases
- Base layout with metadata and global styles
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Source Reference (old codebase -- design tokens only)
@triple-j-auto-investment-main/triple-j-auto-investment-main/tailwind.config.js
@triple-j-auto-investment-main/triple-j-auto-investment-main/package.json
</context>

<acceptance_criteria>

## AC-1: Next.js project runs
```gherkin
Given a fresh project setup
When I run `npm run dev`
Then the dev server starts on localhost:3000 without errors
And the page renders with Triple J branding (colors, fonts)
```

## AC-2: Tailwind configured with design tokens
```gherkin
Given the Tailwind configuration
When I use Triple J color classes (e.g., `bg-tj-green`, `text-tj-gold`)
Then the correct brand colors render
And custom fonts (serif + sans) are available
```

## AC-3: Supabase client ready
```gherkin
Given .env.local with Supabase credentials
When I import from `@/lib/supabase/client` or `@/lib/supabase/server`
Then I get a configured Supabase client
And TypeScript types resolve without errors
```

## AC-4: Folder structure established
```gherkin
Given the project root
When I inspect the src/ directory
Then it contains: app/, lib/, components/, types/, hooks/
And the structure supports App Router conventions
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project and install dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, .gitignore, .env.local.example</files>
  <action>
    Initialize a new Next.js 15 project in the working directory root (NOT inside a subdirectory):
    - Use `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` (or manually create if directory not empty)
    - If directory conflicts, create files manually matching create-next-app output
    - Install additional dependencies:
      - `@supabase/supabase-js` (Supabase client)
      - `@supabase/ssr` (server-side Supabase for App Router)
    - Create `.env.local.example` with placeholder Supabase vars:
      ```
      NEXT_PUBLIC_SUPABASE_URL=your-project-url
      NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
      ```
    - Ensure .gitignore includes .env.local, node_modules, .next
    - Avoid: Do NOT install animation libraries yet (GSAP, Framer Motion) -- that's Phase 9
    - Avoid: Do NOT install i18n libraries yet -- that's Phase 8
  </action>
  <verify>Run `npm run dev` and confirm dev server starts without errors</verify>
  <done>AC-1 partially satisfied: Next.js project initialized and running</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind with Triple J design tokens and global styles</name>
  <files>tailwind.config.ts, src/app/globals.css, src/app/layout.tsx</files>
  <action>
    Configure Tailwind CSS with Triple J brand identity by reading the old tailwind.config.js for color values:

    1. In tailwind.config.ts, extend theme with Triple J colors:
       - Extract exact color values from old `tailwind.config.js` (tj-green, tj-gold, tj-goldMuted, tj-zinc100, tj-zinc400, etc.)
       - Add custom font families: serif (display/headings) and sans (body text)
       - Use Google Fonts: Playfair Display (serif) + Inter or DM Sans (sans)

    2. In globals.css:
       - Import Tailwind layers (@tailwind base, components, utilities)
       - Set base body styles: antialiased, bg color, text color
       - Add CSS custom properties for key brand values if useful
       - Minimal reset (no elaborate custom CSS -- Tailwind handles it)

    3. In layout.tsx:
       - Import Google Fonts via next/font/google (Playfair Display + Inter)
       - Apply font CSS variables to html element
       - Set metadata: title "Triple J Auto Investment", description for dealership
       - Set viewport for mobile-first
       - Wrap children in minimal layout structure

    Avoid: Do NOT add navbar/footer yet -- that's Phase 3
    Avoid: Do NOT add animation-related CSS -- that's Phase 9
  </action>
  <verify>
    Run `npm run dev`, visit localhost:3000.
    Confirm: page renders with dark green background, gold accent text, correct fonts.
    Confirm: Tailwind classes like `bg-tj-green text-tj-gold` work.
  </verify>
  <done>AC-1 and AC-2 satisfied: Branded Next.js page renders with design tokens</done>
</task>

<task type="auto">
  <name>Task 3: Set up folder structure and Supabase clients</name>
  <files>src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/app/page.tsx</files>
  <action>
    1. Create folder structure (empty directories with .gitkeep if needed):
       - src/components/ (shared components)
       - src/components/ui/ (base UI components)
       - src/hooks/ (custom React hooks)
       - src/types/ (TypeScript type definitions)
       - src/lib/ (utilities, clients, helpers)
       - src/lib/supabase/ (Supabase client setup)

    2. Create src/lib/supabase/client.ts:
       - Browser-side Supabase client using `createBrowserClient` from @supabase/ssr
       - Reads NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY from env

    3. Create src/lib/supabase/server.ts:
       - Server-side Supabase client using `createServerClient` from @supabase/ssr
       - Uses cookies() from next/headers for auth persistence
       - Export a `createClient()` function for use in Server Components and Route Handlers

    4. Update src/app/page.tsx:
       - Simple branded landing page showing "Triple J Auto Investment" with the gold/green theme
       - Display: logo placeholder, tagline, phone number (832) 400-9760
       - Display: address "8774 Almeda Genoa Rd, Houston, TX 77075"
       - This is a temporary placeholder -- Homepage is Phase 4

    Avoid: Do NOT create database types yet -- that's Phase 2
    Avoid: Do NOT create any page routes beyond / -- that's Phase 3+
  </action>
  <verify>
    Confirm folder structure exists: `ls src/components src/hooks src/types src/lib/supabase`
    Confirm TypeScript compilation: `npx tsc --noEmit` passes (or `npm run build` succeeds)
    Confirm page.tsx renders the placeholder content at localhost:3000
  </verify>
  <done>AC-3 and AC-4 satisfied: Supabase clients configured, folder structure established</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- triple-j-auto-investment-main/ (old codebase -- read-only reference)
- .paul/ (PAUL framework files -- only STATE.md updated by workflow)

## SCOPE LIMITS
- No animation libraries (GSAP, Framer Motion, Lenis) -- Phase 9
- No i18n setup -- Phase 8
- No database schema or types -- Phase 2
- No navbar, footer, or page routes beyond / -- Phase 3
- No component library (shadcn/ui, etc.) unless explicitly decided later
- Do not add AI-related packages (Gemini, Retell) -- future milestone

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` completes successfully
- [ ] Page renders at localhost:3000 with Triple J branding
- [ ] Tailwind classes `bg-tj-green`, `text-tj-gold`, `font-serif`, `font-sans` work
- [ ] `src/lib/supabase/client.ts` and `server.ts` export valid clients
- [ ] Folder structure: src/{app,components,hooks,types,lib} exist
- [ ] No TypeScript errors (`npx tsc --noEmit`)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All 3 tasks completed
- All verification checks pass
- No errors or warnings introduced
- `npm run dev` shows branded Triple J placeholder page
- Project ready for Phase 2 (Database & Types)
</success_criteria>

<output>
After completion, create `.paul/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
