---
phase: 07-i18n
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/layout/Navbar.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/LanguageToggle.tsx
  - src/components/leads/ContactForm.tsx
  - src/components/inventory/FilterBar.tsx
  - src/components/inventory/SortSelect.tsx
  - src/components/inventory/VehicleCard.tsx
  - src/components/inventory/VehicleSpecs.tsx
  - src/components/inventory/VehicleGallery.tsx
  - src/components/inventory/VehicleInquiryButton.tsx
  - src/components/inventory/PaymentCalculator.tsx
  - src/components/inventory/VinDecoder.tsx
  - src/components/scroll/MaybachSection.tsx
  - src/components/scroll/KeysSection.tsx
  - src/components/scroll/CrestRevealSection.tsx
  - src/app/[locale]/page.tsx
  - src/app/[locale]/inventory/page.tsx
  - src/app/[locale]/inventory/[slug]/page.tsx
  - src/app/[locale]/financing/page.tsx
  - src/app/[locale]/contact/page.tsx
  - src/app/[locale]/vin-decoder/page.tsx
autonomous: false
---

<objective>
## Goal
Integrate translation hooks into all public-facing components and pages, replacing every hardcoded English string with next-intl translation calls. Add a language toggle to the navbar.

## Purpose
Plan 01 set up the infrastructure — this plan makes it live. After this, visiting /es/ shows the full site in natural Spanish, completing the bilingual experience for Triple J's Hispanic customer base.

## Output
- Every public-facing component uses useTranslations() or getTranslations()
- Language toggle (EN/ES) in navbar
- All nav links use locale-aware Link from next-intl
- Full Spanish experience at /es/* routes
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/07-i18n/07-01-PLAN.md (i18n infrastructure this depends on)

## Source Files
@src/components/layout/Navbar.tsx
@src/components/layout/Footer.tsx
@src/components/leads/ContactForm.tsx
@messages/en.json
@messages/es.json
</context>

<skills>
No SPECIAL-FLOWS.md — skills section omitted.
</skills>

<acceptance_criteria>

## AC-1: All Public Strings Translated
```gherkin
Given the site is loaded at /es/
When any public page or component renders
Then all visible text is in Spanish
And no English strings remain (except brand name "Triple J Auto Investment")
```

## AC-2: Language Toggle Works
```gherkin
Given a user is browsing at /en/inventory
When they click the language toggle to ES
Then they are navigated to /es/inventory
And the page content switches to Spanish
And the toggle reflects the current language
```

## AC-3: Navigation Links Are Locale-Aware
```gherkin
Given the site is loaded at /es/
When a user clicks "Inventario" in the navbar
Then they navigate to /es/inventory (not /inventory or /en/inventory)
```

## AC-4: Build Passes
```gherkin
Given all components use translation hooks
When `next build` runs
Then it completes with zero errors
And both /en and /es routes are generated
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create language toggle and update Navbar/Footer with translations</name>
  <files>src/components/layout/LanguageToggle.tsx, src/components/layout/Navbar.tsx, src/components/layout/Footer.tsx</files>
  <action>
    1. Create src/components/layout/LanguageToggle.tsx:
       - Client component ("use client")
       - Use useLocale() from next-intl and usePathname/useRouter from next-intl navigation
       - Display current locale (EN/ES) with a toggle button
       - Styling: match navbar aesthetic — font-accent, uppercase, small text, gold accent
       - On click: router.replace(pathname, { locale: newLocale })
       - Minimum 44px touch target

    2. Update Navbar.tsx:
       - Import useTranslations from next-intl
       - Replace hardcoded link labels with t('nav.inventory'), t('nav.financing'), etc.
       - Replace Link from next/link with Link from src/i18n/navigation.ts (locale-aware)
       - Add LanguageToggle component in desktop right section (before phone) and mobile menu
       - Update aria-labels with translation keys

    3. Update Footer.tsx:
       - Server component: use getTranslations from next-intl/server
       - Replace all hardcoded strings (brand, tagline, links, hours, address, license text)
       - Replace Link from next/link with locale-aware Link
       - Keep "Dealer Login" link pointing to /admin/login (no locale prefix)

    Avoid:
    - Do NOT change the visual design/layout of navbar or footer
    - Do NOT modify animation behavior
    - Keep the Rolls Royce centered layout exactly as-is
  </action>
  <verify>
    - LanguageToggle.tsx exists and exports a client component
    - Navbar shows translated labels when locale changes
    - Footer shows translated content
    - All nav links include locale prefix in href
  </verify>
  <done>AC-2 satisfied (language toggle), AC-3 satisfied (locale-aware links)</done>
</task>

<task type="auto">
  <name>Task 2: Integrate translations into all remaining components and pages</name>
  <files>
    src/components/leads/ContactForm.tsx,
    src/components/inventory/FilterBar.tsx,
    src/components/inventory/SortSelect.tsx,
    src/components/inventory/VehicleCard.tsx,
    src/components/inventory/VehicleSpecs.tsx,
    src/components/inventory/VehicleGallery.tsx,
    src/components/inventory/VehicleInquiryButton.tsx,
    src/components/inventory/PaymentCalculator.tsx,
    src/components/inventory/VinDecoder.tsx,
    src/components/scroll/MaybachSection.tsx,
    src/components/scroll/KeysSection.tsx,
    src/components/scroll/CrestRevealSection.tsx,
    src/app/[locale]/page.tsx,
    src/app/[locale]/inventory/page.tsx,
    src/app/[locale]/inventory/[slug]/page.tsx,
    src/app/[locale]/financing/page.tsx,
    src/app/[locale]/contact/page.tsx,
    src/app/[locale]/vin-decoder/page.tsx
  </files>
  <action>
    For CLIENT components (components with "use client"):
    - Import useTranslations from next-intl
    - Call const t = useTranslations('namespace') at top of component
    - Replace every hardcoded string with t('key')
    - For strings with variables: t('key', { variable: value })

    For SERVER components (pages, server-rendered components):
    - Import getTranslations from next-intl/server
    - Call const t = await getTranslations('namespace')
    - Replace every hardcoded string with t('key')

    Components to update (by priority):
    1. ContactForm.tsx — form labels, placeholders, success/error messages, submit button
    2. FilterBar.tsx — search placeholder, filter labels, "All Makes", "All Years", "Any Price"
    3. SortSelect.tsx — sort option labels
    4. VehicleCard.tsx — "Photo Coming Soon", miles unit, "View Details"
    5. VehicleSpecs.tsx — spec labels (VIN, Mileage, Transmission, etc.)
    6. VehicleInquiryButton.tsx — CTA text
    7. PaymentCalculator.tsx — all labels, disclaimer text
    8. VinDecoder.tsx — instructions, button text, spec labels
    9. MaybachSection.tsx — phase label, heading, description, CTA
    10. KeysSection.tsx — phase label, heading, description, CTA
    11. CrestRevealSection.tsx — tagline text

    Pages to update:
    1. [locale]/inventory/page.tsx — "The Collection" title, empty state
    2. [locale]/inventory/[slug]/page.tsx — spec section headers, metadata
    3. [locale]/financing/page.tsx — all step labels, benefits, CTA section
    4. [locale]/contact/page.tsx — intro text, contact info labels, hours
    5. [locale]/vin-decoder/page.tsx — feature cards, instructions
    6. [locale]/page.tsx — if any text outside scroll sections

    Ensure all Link components from next/link are switched to locale-aware Link from src/i18n/navigation.ts.

    Avoid:
    - Do NOT translate the brand name "Triple J Auto Investment"
    - Do NOT translate admin components
    - Do NOT change component structure or styling
    - Do NOT add new translation keys — use only what's in en.json/es.json from Plan 01
    - If a key is missing from the JSON files, add it to BOTH en.json and es.json
  </action>
  <verify>
    - Every public component/page imports from next-intl
    - No hardcoded English user-facing strings remain in public components
    - `npx next build` passes with zero errors
    - Both /en/* and /es/* routes render correctly
  </verify>
  <done>AC-1 satisfied (all strings translated), AC-4 satisfied (build passes)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete bilingual site:
    - Language toggle (EN/ES) in navbar
    - All public pages and components use translation hooks
    - Full Spanish experience at /es/* routes
    - Locale-aware navigation throughout
  </what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit http://localhost:3000/en — full English experience
    3. Click language toggle to ES — redirects to /es, all content in Spanish
    4. Navigate to /es/inventory — Spanish labels, filters, vehicle cards
    5. Open a vehicle detail page — Spanish specs, payment calculator, CTAs
    6. Visit /es/financing — Spanish steps, benefits, CTA
    7. Visit /es/contact — Spanish form labels, contact info
    8. Test mobile: hamburger menu shows translated links + language toggle
    9. Visit /admin/login — still works, English, no locale prefix
    10. Review Spanish copy for naturalness and cultural fit
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/app/admin/* (admin stays English-only)
- src/app/api/* (API routes unchanged)
- src/lib/* (data layer, Supabase client)
- public/* (static assets)
- Visual design, layout, animations, color scheme
- Component structure and behavior

## SCOPE LIMITS
- Only replace strings — no visual redesign
- No RTL language support
- No language-specific SEO meta tags (Phase 8 handles SEO)
- No dynamic content translation (vehicle data from database stays as-is)
- Admin dashboard remains English-only for v0.1

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Language toggle visible in navbar (desktop + mobile)
- [ ] Clicking toggle switches locale and all visible text
- [ ] /en/* shows complete English site
- [ ] /es/* shows complete Spanish site
- [ ] No hardcoded English strings in public components (except brand name)
- [ ] All navigation links are locale-aware
- [ ] /admin/* still works without locale prefix
- [ ] `npx next build` passes with zero errors
</verification>

<success_criteria>
- Every public-facing string rendered via next-intl translation hooks
- Language toggle functional in navbar
- Full Spanish experience at /es/* routes
- Build passes, no regressions
- Phase 7 (Bilingual/i18n) complete
</success_criteria>

<output>
After completion, create `.paul/phases/07-i18n/07-02-SUMMARY.md`
</output>
