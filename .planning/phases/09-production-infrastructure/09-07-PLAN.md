---
phase: 09-production-infrastructure
plan: 07
type: execute
wave: 5
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05", "09-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Admin can log in, create a vehicle, and see it in the inventory list"
    - "Customer receives real SMS OTP and can log in to the status tracker"
    - "Edge Functions fire on schedule -- notification queue and plate alerts verified"
    - "File uploads work -- PDFs, photos save and display"
    - "Site loads at custom domain with valid SSL, no console errors from missing env vars"
  artifacts:
    - path: "(Production site)"
      provides: "Fully functional production deployment verified end-to-end"
  key_links:
    - from: "Admin login"
      to: "Vehicle CRUD"
      via: "is_admin flag enables admin routes"
      pattern: "is_admin.*true"
    - from: "Customer phone"
      to: "SMS OTP"
      via: "Supabase Auth -> Twilio"
      pattern: "signInWithOtp.*phone"
    - from: "File upload"
      to: "Storage bucket"
      via: "Supabase Storage client"
      pattern: "supabase.*storage.*upload"
    - from: "pg_cron schedule"
      to: "Edge Function invocation"
      via: "net.http_post via app.settings URL"
      pattern: "cron\\.job_run_details.*succeeded"
---

<objective>
Run a comprehensive end-to-end smoke test on the production site to verify all v1 workflows work correctly.

Purpose: This is the final verification that everything from Plans 01-06 is working together. Every success criterion from the phase goal must be verified on the live production site. This plan covers DEPLOY-13 (end-to-end smoke test).

Output: All 5 phase success criteria verified. Production is live and operational.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-production-infrastructure/09-RESEARCH.md
@.planning/phases/09-production-infrastructure/09-05-SUMMARY.md
@.planning/phases/09-production-infrastructure/09-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete production deployment -- Supabase backend, Edge Functions, storage, Vercel frontend, custom domain, vehicle data migration, credential cleanup</what-built>
  <how-to-verify>
    Run through each test below on the PRODUCTION site at https://triplejautoinvestment.com. Report results for each.

    **Test 1: Admin Login + Vehicle CRUD (Success Criterion 1)**
    1. Go to https://triplejautoinvestment.com
    2. Navigate to admin login
    3. Log in with the admin credentials from Plan 02
    4. Expected: Admin dashboard loads with no errors
    5. Create a NEW test vehicle (any make/model/year/price)
    6. Expected: Vehicle appears in the inventory list
    7. Delete the test vehicle
    8. Expected: Vehicle is removed from inventory
    PASS / FAIL: ___

    **Test 2: Customer SMS OTP (Success Criterion 2)**
    1. Go to the customer portal / status tracker
    2. Enter YOUR phone number (the one registered in Twilio)
    3. Expected: You receive an SMS with a 6-digit OTP code
    4. Enter the OTP code
    5. Expected: You are logged in to the customer portal
    NOTE: If A2P 10DLC is still pending, this may fail for non-verified numbers. Test with your Twilio-verified number.
    PASS / FAIL / BLOCKED (A2P pending): ___

    **Test 3: Edge Functions + Cron (Success Criterion 3)**
    1. Go to Supabase Dashboard -> SQL Editor
    2. Run:
    ```sql
    SELECT jobname, status, return_message, start_time
    FROM cron.job_run_details
    ORDER BY start_time DESC
    LIMIT 10;
    ```
    3. Expected: Recent entries with status = 'succeeded' for both `process-notification-queue` and `check-plate-alerts`
    4. If any show 'failed', check the return_message for the error
    PASS / FAIL: ___

    **Test 4: File Uploads (Success Criterion 4)**
    1. As admin on the production site, navigate to a rental or registration workflow
    2. Try uploading a test PDF document (any PDF)
    3. Expected: Upload succeeds, file appears in the UI
    4. Try uploading a test photo (any image)
    5. Expected: Upload succeeds, image displays
    6. Verify in Supabase Dashboard -> Storage that files appear in the correct bucket
    PASS / FAIL: ___

    **Test 5: Domain + SSL + Console (Success Criterion 5)**
    1. Visit https://triplejautoinvestment.com
    2. Check: SSL padlock icon present (no certificate warnings)
    3. Open DevTools (F12) -> Console
    4. Navigate through: Home, Inventory, About, Contact, Services pages
    5. Expected: NO console errors referencing missing environment variables (VITE_SUPABASE_URL, etc.)
    6. Some console.log output is expected (will be cleaned in Phase 11) -- look specifically for ERROR level messages
    PASS / FAIL: ___

    **Test 6: Resend Domain Verification (Bonus)**
    1. Check Resend Dashboard -> Domains
    2. Is `triplejautoinvestment.com` showing "Verified" status?
    3. If still "Pending" after 48 hours, verify DNS records are correct at Namecheap
    VERIFIED / STILL PENDING: ___

    **Report all results.** The phase is complete when Tests 1, 3, 4, and 5 pass. Test 2 may be blocked by A2P 10DLC status. Test 6 depends on DNS propagation timing.
  </how-to-verify>
  <resume-signal>Report results for each test: "Test 1: PASS, Test 2: PASS/BLOCKED, Test 3: PASS, Test 4: PASS, Test 5: PASS, Test 6: VERIFIED/PENDING"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Document production deployment details</name>
  <files>
    (No files modified -- summary only)
  </files>
  <action>
    Based on the smoke test results, create the phase summary documenting:

    1. Production URLs and access points
    2. Smoke test results (pass/fail/blocked for each criterion)
    3. Any issues found and their severity
    4. Follow-up items (e.g., Resend still pending, A2P 10DLC blocking OTP)
    5. Credential rotation status (what was rotated, what still needs attention)

    If any critical tests failed, document the failure and recommend a gap closure plan.

    Update STATE.md with:
    - Phase 9 status
    - Production URLs
    - Any blockers for Phase 10
  </action>
  <verify>Phase summary accurately reflects production status.</verify>
  <done>Production deployment is documented. STATE.md updated. Phase 9 complete (or gap closure plans identified).</done>
</task>

</tasks>

<verification>
All 5 phase success criteria from the roadmap verified:
1. Admin can log in to production, create vehicle, see it in inventory
2. Customer receives real SMS OTP and can log in
3. Edge Functions fire on schedule -- notification queue and plate alerts verified
4. File uploads work -- PDFs, photos save and display
5. Site loads at custom domain with valid SSL, no console errors from missing env vars
</verification>

<success_criteria>
1. Tests 1, 3, 4, 5 all PASS
2. Test 2 either PASS or documented as BLOCKED with workaround (A2P 10DLC)
3. Production deployment details documented in summary
4. STATE.md updated with Phase 9 completion status
5. Any follow-up items (Resend verification, image migration) documented
</success_criteria>

<output>
After completion, create `.planning/phases/09-production-infrastructure/09-07-SUMMARY.md`
</output>
