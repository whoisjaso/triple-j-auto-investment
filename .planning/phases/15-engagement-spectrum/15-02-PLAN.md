---
phase: 15-engagement-spectrum
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - triple-j-auto-investment-main/components/SaveButton.tsx
  - triple-j-auto-investment-main/components/PaymentCalculator.tsx
  - triple-j-auto-investment-main/components/PhoneCaptureForm.tsx
  - triple-j-auto-investment-main/pages/Inventory.tsx
autonomous: true

must_haves:
  truths:
    - "SaveButton renders a heart icon that toggles saved state on click with no login or form"
    - "SaveButton persists state in localStorage via useSavedVehicles hook and survives page reload"
    - "PaymentCalculator shows interactive down payment and term inputs with real-time monthly estimate"
    - "PaymentCalculator requires zero personal information to use"
    - "PhoneCaptureForm collects a phone number with validation and submits a vehicle lead"
    - "PhoneCaptureForm is reusable across price alert, similar vehicles, and vehicle report actions"
    - "Inventory page shows a 'Saved' filter that displays only saved vehicles"
    - "All components are bilingual via useLanguage"
  artifacts:
    - path: "triple-j-auto-investment-main/components/SaveButton.tsx"
      provides: "Heart icon save/unsave toggle for vehicles"
      min_lines: 30
    - path: "triple-j-auto-investment-main/components/PaymentCalculator.tsx"
      provides: "Interactive payment calculator with down payment and term controls"
      min_lines: 80
    - path: "triple-j-auto-investment-main/components/PhoneCaptureForm.tsx"
      provides: "Reusable phone-only lead capture form for Level 1 actions"
      min_lines: 60
    - path: "triple-j-auto-investment-main/pages/Inventory.tsx"
      provides: "Saved vehicles filter and SaveButton on cards"
      contains: "SaveButton"
  key_links:
    - from: "triple-j-auto-investment-main/components/SaveButton.tsx"
      to: "triple-j-auto-investment-main/hooks/useSavedVehicles.ts"
      via: "hook import"
      pattern: "import.*useSavedVehicles"
    - from: "triple-j-auto-investment-main/components/PaymentCalculator.tsx"
      to: "triple-j-auto-investment-main/services/marketEstimateService.ts"
      via: "estimateMonthlyPayment import"
      pattern: "import.*estimateMonthlyPayment"
    - from: "triple-j-auto-investment-main/components/PhoneCaptureForm.tsx"
      to: "triple-j-auto-investment-main/services/vehicleLeadService.ts"
      via: "createVehicleLead import"
      pattern: "import.*createVehicleLead"
    - from: "triple-j-auto-investment-main/pages/Inventory.tsx"
      to: "triple-j-auto-investment-main/components/SaveButton.tsx"
      via: "component import"
      pattern: "import.*SaveButton"
---

<objective>
Build Level 0 and Level 1 engagement components: save/favorite button, interactive payment calculator, and phone-only lead capture form.

Purpose: These are the lowest-friction engagement actions. Level 0 requires zero information (save + calculator). Level 1 requires only a phone number (price alert, similar vehicles, vehicle report). All components are reusable building blocks assembled into the vehicle detail page in Plan 03.
Output: SaveButton, PaymentCalculator, PhoneCaptureForm components, plus inventory page integration for saved vehicles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-engagement-spectrum/15-CONTEXT.md
@.planning/phases/15-engagement-spectrum/15-RESEARCH.md
@.planning/phases/15-engagement-spectrum/15-01-SUMMARY.md

Key existing files:
@triple-j-auto-investment-main/hooks/useSavedVehicles.ts
@triple-j-auto-investment-main/services/vehicleLeadService.ts
@triple-j-auto-investment-main/services/marketEstimateService.ts
@triple-j-auto-investment-main/context/Store.tsx
@triple-j-auto-investment-main/context/LanguageContext.tsx
@triple-j-auto-investment-main/utils/translations.ts
@triple-j-auto-investment-main/pages/Inventory.tsx
@triple-j-auto-investment-main/pages/VehicleDetail.tsx
@triple-j-auto-investment-main/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SaveButton component</name>
  <files>
    triple-j-auto-investment-main/components/SaveButton.tsx
  </files>
  <action>
    Create NEW file: `components/SaveButton.tsx`

    **Imports:**
    - React from 'react'
    - motion from 'framer-motion'
    - Heart from 'lucide-react'
    - useSavedVehicles from '../hooks/useSavedVehicles'

    **Props:**
    ```typescript
    interface SaveButtonProps {
      vehicleId: string;
      size?: 'sm' | 'md' | 'lg';
      className?: string;
    }
    ```

    **Behavior:**
    - Calls `useSavedVehicles()` hook to get `isSaved` and `toggleSave`
    - On click: calls `toggleSave(vehicleId)` -- single tap, no confirmation
    - Heart icon: filled (solid) when saved, outlined (stroke) when not saved
    - `e.stopPropagation()` and `e.preventDefault()` on click to prevent card navigation when used on inventory cards

    **Visual design:**
    - Size variants:
      - sm: `w-8 h-8` button, `size={14}` icon (for inventory cards)
      - md: `w-10 h-10` button, `size={18}` icon (default)
      - lg: `w-12 h-12` button, `size={22}` icon (for detail page)
    - Saved state: Heart filled with `fill-red-500 text-red-500`
    - Unsaved state: Heart stroke only with `text-gray-400 hover:text-red-400`
    - Subtle scale animation on toggle: `motion.button` with `whileTap={{ scale: 0.85 }}`
    - Pop animation on save: `animate={{ scale: [1, 1.3, 1] }}` with `transition={{ duration: 0.3 }}` when toggling to saved
    - Background: `bg-black/60 backdrop-blur-sm rounded-full` for overlay contexts (cards)
    - Accessible: `aria-label` set to save/unsave text, minimum 44px touch target via padding
    - Focus ring: `focus:ring-2 focus:ring-red-500/50 focus:outline-none`

    **Important:** The component should be lightweight -- it delegates all state management to the useSavedVehicles hook. No internal state needed except for the pop animation key.
  </action>
  <verify>
    Run `npx tsc --noEmit` from the triple-j-auto-investment-main directory. Zero type errors.
    Verify file exists and imports useSavedVehicles hook.
    Verify Heart icon import from lucide-react.
  </verify>
  <done>
    SaveButton renders a heart icon toggle. Click saves/unsaves vehicle via localStorage hook. Animated pop on save. Three size variants. Accessible with proper aria-label and touch targets.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PaymentCalculator component</name>
  <files>
    triple-j-auto-investment-main/components/PaymentCalculator.tsx
  </files>
  <action>
    Create NEW file: `components/PaymentCalculator.tsx`

    **Imports:**
    - React, useState, useMemo from 'react'
    - motion, AnimatePresence from 'framer-motion'
    - Calculator, ChevronDown, ChevronUp from 'lucide-react'
    - estimateMonthlyPayment from '../services/marketEstimateService'
    - useLanguage from '../context/LanguageContext'

    **Props:**
    ```typescript
    interface PaymentCalculatorProps {
      price: number;
      className?: string;
    }
    ```

    **State:**
    - `isOpen: boolean` -- collapsed/expanded toggle (starts collapsed)
    - `downPayment: number` -- slider value, default 500, range 0-2000, step 100
    - `termMonths: number` -- selectable: 12, 18, 24, 36 (default 24)

    **Layout (collapsed state):**
    - A clickable bar with Calculator icon + "Payment Calculator" text + chevron
    - Shows the default estimate: "Est. $XXX/mo"
    - Click expands to full calculator
    - Design: `bg-white/[0.03] border border-white/[0.06] rounded-lg p-4 cursor-pointer`

    **Layout (expanded state):**
    - AnimatePresence expand animation (height from 0)
    - **Down Payment section:**
      - Label: "Down Payment" from translations
      - Display current value: "$500"
      - Range input (slider): min=0, max=2000, step=100
      - Styled slider: Tailwind custom styling with `accent-tj-gold` or custom CSS
      - Quick-select buttons: $0, $500, $1000, $1500, $2000
    - **Term section:**
      - Label: "Loan Term" from translations
      - Four buttons in a row: 12, 18, 24, 36 months
      - Selected button: `bg-tj-gold text-black`
      - Unselected: `bg-white/[0.04] text-gray-400 hover:bg-white/[0.08]`
    - **Result section:**
      - Large display: "Est. $XXX/mo" in `font-display text-3xl text-white`
      - Subtitle: "with $500 down - 24 months" from translations
      - Disclaimer: small text from t.engagement.paymentDisclaimer
    - **CTA:** "Apply for Financing" link to /finance in subtle button style

    **Calculation:**
    Use `estimateMonthlyPayment(price, downPayment, termMonths)` from marketEstimateService. Recalculate on every slider/button change using useMemo.

    **Design system:**
    - Follow existing bg-black, text-white, border-white/[0.06] patterns
    - Gold accent for interactive elements
    - text-[10px] uppercase tracking-[0.2em] for labels
    - font-display for the monthly estimate number
    - Responsive: full width on mobile, max-w within container
    - All text bilingual via t.engagement.*
  </action>
  <verify>
    Run `npx tsc --noEmit` from the triple-j-auto-investment-main directory. Zero type errors.
    Verify estimateMonthlyPayment import.
    Verify useLanguage import for bilingual support.
    File should be 80+ lines.
  </verify>
  <done>
    PaymentCalculator shows an expandable section with interactive down payment slider, term selection buttons, and real-time monthly estimate. Zero personal information required. Uses existing estimateMonthlyPayment. Bilingual. Follows design system.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create PhoneCaptureForm component</name>
  <files>
    triple-j-auto-investment-main/components/PhoneCaptureForm.tsx
  </files>
  <action>
    Create NEW file: `components/PhoneCaptureForm.tsx`

    **Imports:**
    - React, useState from 'react'
    - motion, AnimatePresence from 'framer-motion'
    - Phone, CheckCircle, Loader2, AlertCircle from 'lucide-react'
    - useLanguage from '../context/LanguageContext'
    - useStore from '../context/Store'
    - createVehicleLead, isValidPhone from '../services/vehicleLeadService'
    - LeadActionType from '../types'

    **Props:**
    ```typescript
    interface PhoneCaptureFormProps {
      actionType: LeadActionType;
      vehicleId: string;
      vehicleVin: string;
      label: string;
      description: string;
      icon: React.ReactNode;
      className?: string;
    }
    ```

    **State:**
    - `isExpanded: boolean` -- false by default
    - `phone: string` -- phone input value
    - `status: 'idle' | 'submitting' | 'success' | 'error'`
    - `error: string` -- validation error message

    **Layout (collapsed):**
    - A button with the provided icon + label + description text
    - Click expands to show the phone input
    - Design: `bg-white/[0.03] border border-white/[0.06] rounded-lg p-4`
    - Hover: `hover:bg-white/[0.05] hover:border-white/[0.10]`
    - Transition: `transition-all duration-200`

    **Layout (expanded):**
    - AnimatePresence slide-down animation
    - Phone input with country code prefix (+1) shown as static text
    - Input: `bg-white/[0.04] border border-white/[0.08] rounded text-white px-4 py-3`
    - Phone formatting as user types: (XXX) XXX-XXXX
    - Submit button: `bg-tj-gold text-black` with t.engagement.submit
    - Validation: show error message if phone is invalid on submit attempt

    **Phone input formatting:**
    As the user types digits, format automatically:
    - 3 digits: (832)
    - 6 digits: (832) 400
    - 10 digits: (832) 400-9760
    Strip non-digits before validation and submission.

    **Submission:**
    1. Validate phone with `isValidPhone(phone)`
    2. If invalid, show t.engagement.invalidPhone error
    3. If valid, set status='submitting'
    4. Create lead: `const lead = createVehicleLead({ actionType, phone: digits, vehicleId, vehicleVin })`
    5. Call `addLead(lead)` from Store context
    6. On success: set status='success'
    7. On error: set status='error'

    **Success state:**
    - Replace form with CheckCircle icon + t.engagement.submitted + t.engagement.wellBeInTouch
    - Green accent: `text-green-400`
    - Auto-collapse after 3 seconds (return to collapsed button state with subtle "Submitted" indicator)

    **Error state:**
    - Show AlertCircle + error message below form
    - Allow retry

    **Accessibility:**
    - Input: `aria-label` from translations
    - Button: minimum 44px height
    - Focus management: auto-focus phone input when expanded
    - Keyboard: Enter submits, Escape collapses
  </action>
  <verify>
    Run `npx tsc --noEmit` from the triple-j-auto-investment-main directory. Zero type errors.
    Verify createVehicleLead import from vehicleLeadService.
    Verify addLead usage from Store context.
    File should be 60+ lines.
  </verify>
  <done>
    PhoneCaptureForm is a reusable component that expands to show a phone input, validates, creates a vehicle lead, and shows success/error states. Configurable via props for different Level 1 action types (price alert, similar vehicles, vehicle report). Bilingual. Accessible.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add SaveButton and saved filter to Inventory page</name>
  <files>
    triple-j-auto-investment-main/pages/Inventory.tsx
  </files>
  <action>
    Make THREE additions to Inventory.tsx. These are conservative changes -- no layout redesign.

    **Import additions:**
    - `import { SaveButton } from '../components/SaveButton';`
    - `import { useSavedVehicles } from '../hooks/useSavedVehicles';`

    **Change 1: Add SaveButton to vehicle cards**
    In the VehicleCard component, inside the image overlay area (the absolute-positioned badges area at the top of each card), add the SaveButton in the top-right corner:

    ```tsx
    <div className="absolute top-3 right-3 z-20">
      <SaveButton vehicleId={vehicle.id} size="sm" />
    </div>
    ```

    Position it so it doesn't overlap with existing badges (status badge is top-left). The SaveButton handles its own click propagation prevention.

    **Change 2: Add "Saved" filter option**
    In the filter/sort controls area of the Inventory page, add a "Saved" toggle button alongside existing filters. Find the filter section and add:

    - Call `useSavedVehicles()` at the Inventory page component level (NOT inside VehicleCard) to get `savedIds` and `savedCount`
    - Add a filter button: "Saved ({count})" that toggles a `showSavedOnly` state
    - When active: filter the vehicles array to only show vehicles whose ID is in `savedIds`
    - When inactive: show all vehicles (normal behavior)
    - Button style when active: `bg-red-500/20 text-red-400 border-red-500/30`
    - Button style when inactive: `bg-white/[0.04] text-gray-400 border-white/[0.08]`
    - Heart icon prefix on the button
    - Only show the Saved filter button when `savedCount > 0`

    **Change 3: Empty state for saved filter**
    When `showSavedOnly` is true but no saved vehicles exist (user unsaved all), show a subtle empty state:
    - Heart icon (outlined) + t.engagement.noSavedVehicles text + t.engagement.tapToSave subtext
    - Style: centered, text-gray-500, py-12

    **DO NOT change** the modal behavior, card layout structure, existing filters, sorting, or any other existing Inventory.tsx functionality. Only these three additions.
  </action>
  <verify>
    Run `npx tsc --noEmit` from the triple-j-auto-investment-main directory. Zero type errors.
    Run `npx vite build` from the triple-j-auto-investment-main directory. Build succeeds.
    Verify SaveButton import: grep for "SaveButton" in pages/Inventory.tsx.
    Verify useSavedVehicles import: grep for "useSavedVehicles" in pages/Inventory.tsx.
    Verify saved filter state: grep for "showSavedOnly" in pages/Inventory.tsx.
  </verify>
  <done>
    Inventory page has SaveButton hearts on every vehicle card (top-right of image). A "Saved" filter button appears when vehicles are saved, filtering to show only saved vehicles. Empty state shown when saved filter is active but no vehicles are saved. All existing functionality preserved.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx vite build` succeeds
- SaveButton.tsx exists with heart icon toggle and localStorage persistence
- PaymentCalculator.tsx exists with interactive controls and real-time estimate
- PhoneCaptureForm.tsx exists with expandable phone capture and lead creation
- Inventory page shows SaveButton on all vehicle cards
- Inventory page has "Saved" filter that works correctly
- All components use translations from t.engagement.*
- All interactive elements meet 44px minimum touch targets
- All components handle loading, success, and error states
</verification>

<success_criteria>
A visitor can tap a heart icon on any vehicle card or detail page to save it with no login required. Saved vehicles persist on page reload and can be filtered on the inventory page. The payment calculator shows an interactive estimate with no personal info required. Phone capture forms collect a number and create vehicle leads in Supabase for Level 1 actions.
</success_criteria>

<output>
After completion, create `.planning/phases/15-engagement-spectrum/15-02-SUMMARY.md`
</output>
