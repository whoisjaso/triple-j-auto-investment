---
phase: 11-production-polish
plan: 08
type: execute
wave: 5
depends_on: []
files_modified:
  - triple-j-auto-investment-main/pages/Inventory.tsx
  - triple-j-auto-investment-main/pages/VinLookup.tsx
  - triple-j-auto-investment-main/pages/CustomerLogin.tsx
  - triple-j-auto-investment-main/pages/RegistrationTracker.tsx
  - triple-j-auto-investment-main/pages/NotFound.tsx
  - triple-j-auto-investment-main/pages/Legal.tsx
  - triple-j-auto-investment-main/pages/Policies.tsx
  - triple-j-auto-investment-main/pages/Login.tsx
  - triple-j-auto-investment-main/pages/CustomerStatusTracker.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Zero instances of text-gray-500 or text-gray-600 on readable text across all 9 customer-facing pages"
    - "All body text on dark backgrounds meets WCAG AA 4.5:1 minimum contrast ratio"
  artifacts:
    - path: "triple-j-auto-investment-main/pages/Inventory.tsx"
      provides: "WCAG AA contrast on all 42 former gray-500/600 instances"
      contains: "text-gray-400"
    - path: "triple-j-auto-investment-main/pages/VinLookup.tsx"
      provides: "WCAG AA contrast on all 20 former gray-500/600 instances"
      contains: "text-gray-400"
    - path: "triple-j-auto-investment-main/pages/CustomerLogin.tsx"
      provides: "WCAG AA contrast on all 6 former gray-500/600 instances"
      contains: "text-gray-400"
    - path: "triple-j-auto-investment-main/pages/RegistrationTracker.tsx"
      provides: "WCAG AA contrast on all 7 former gray-500/600 instances"
      contains: "text-gray-400"
    - path: "triple-j-auto-investment-main/pages/NotFound.tsx"
      provides: "WCAG AA contrast on all 3 former gray-500 instances"
      contains: "text-gray-400"
    - path: "triple-j-auto-investment-main/pages/Legal.tsx"
      provides: "WCAG AA contrast on all 3 former gray-500 instances"
      contains: "text-gray-400"
    - path: "triple-j-auto-investment-main/pages/Policies.tsx"
      provides: "WCAG AA contrast on all 3 former gray-500 instances"
      contains: "text-gray-400"
    - path: "triple-j-auto-investment-main/pages/Login.tsx"
      provides: "WCAG AA contrast on all 2 former gray-500/600 instances"
      contains: "text-gray-400"
    - path: "triple-j-auto-investment-main/pages/CustomerStatusTracker.tsx"
      provides: "WCAG AA contrast on all 2 former gray-500 instances"
      contains: "text-gray-400"
  key_links: []
---

<objective>
Fix remaining WCAG AA color contrast failures across 9 customer-facing pages by replacing text-gray-500 and text-gray-600 with text-gray-400 on all readable text.

Purpose: Phase 11-05 cleaned contrast on some pages but missed these 9 files (87+ instances total). The verification report flagged this as a WARNING-severity gap. text-gray-400 (#9CA3AF) on black achieves ~5.5:1 contrast ratio, passing WCAG AA. This is a mechanical find-and-replace following the exact pattern established in 11-05.

Output: 9 page files updated with WCAG AA compliant text colors.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-production-polish/11-VERIFICATION.md
@.planning/phases/11-production-polish/11-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace text-gray-500/600 on high-count pages (Inventory + VinLookup)</name>
  <files>
    triple-j-auto-investment-main/pages/Inventory.tsx
    triple-j-auto-investment-main/pages/VinLookup.tsx
  </files>
  <action>
    These two pages account for 62 of the 87+ remaining instances. Apply the same replacement pattern used in 11-05.

    **Replacement rules (established in 11-05):**
    - `text-gray-500` on readable text (labels, descriptions, form text, spec values, status text) -> `text-gray-400`
    - `text-gray-600` on readable text -> `text-gray-400`
    - `text-gray-500` on placeholder text inside inputs -> `placeholder:text-gray-500` is acceptable (placeholders are hints, not content)
    - `text-gray-600` used as a border color or background -> leave unchanged (not text contrast)

    **For Inventory.tsx (42 instances):**
    Read the file. Search for every `text-gray-500` and `text-gray-600` class. For each instance:
    - If it applies to text a user needs to read (vehicle specs, labels, descriptions, prices, status indicators, filter labels, sort text, modal content) -> replace with `text-gray-400`
    - If it's `placeholder:text-gray-500` on an input -> leave as-is
    - If it appears inside a `hover:text-gray-500` or similar interactive state -> replace with `hover:text-gray-400`

    **For VinLookup.tsx (20 instances):**
    Same approach. These are likely spec labels, VIN decode results, and form helper text. Replace all readable text instances with `text-gray-400`.

    **Important:**
    - Do NOT touch any admin/ page files (out of scope per 11-05 decision)
    - Do NOT change text-gray-300 or lighter (already passing)
    - Do NOT change border-gray-500/600 or bg-gray-500/600 (those are not text contrast)
    - Preserve all other Tailwind classes on the same element
  </action>
  <verify>
    After replacement, run counts on each file:
    - `grep -c "text-gray-500\|text-gray-600" pages/Inventory.tsx` should return 0 (or only placeholder instances)
    - `grep -c "text-gray-500\|text-gray-600" pages/VinLookup.tsx` should return 0 (or only placeholder instances)
    - Run `npm run build` in triple-j-auto-investment-main to confirm no errors
  </verify>
  <done>
    Inventory.tsx and VinLookup.tsx have zero text-gray-500/600 on readable text. All body text meets WCAG AA 4.5:1 contrast ratio.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace text-gray-500/600 on remaining 7 pages</name>
  <files>
    triple-j-auto-investment-main/pages/CustomerLogin.tsx
    triple-j-auto-investment-main/pages/RegistrationTracker.tsx
    triple-j-auto-investment-main/pages/NotFound.tsx
    triple-j-auto-investment-main/pages/Legal.tsx
    triple-j-auto-investment-main/pages/Policies.tsx
    triple-j-auto-investment-main/pages/Login.tsx
    triple-j-auto-investment-main/pages/CustomerStatusTracker.tsx
  </files>
  <action>
    Apply the same replacement pattern to the remaining 7 customer-facing pages (25 instances total).

    **Per-file expected counts from VERIFICATION.md:**
    - CustomerLogin.tsx: 6 instances (form labels, helper text)
    - RegistrationTracker.tsx: 7 instances (status labels, tracking info)
    - NotFound.tsx: 3 instances (description text)
    - Legal.tsx: 3 instances (section labels)
    - Policies.tsx: 3 instances (policy text)
    - Login.tsx: 2 instances (form text)
    - CustomerStatusTracker.tsx: 2 instances (status text)

    **For each file:**
    1. Read the file
    2. Find every `text-gray-500` and `text-gray-600` class on readable text
    3. Replace with `text-gray-400`
    4. Preserve all other classes and element structure

    **Same rules as Task 1:**
    - Readable text (labels, descriptions, helper text, status text) -> `text-gray-400`
    - `placeholder:text-gray-500` on inputs -> leave as-is
    - Border/background gray classes -> leave as-is
    - `hover:text-gray-500` -> `hover:text-gray-400`
    - Do NOT touch admin pages

    **Special note on Login.tsx:**
    Per 11-05 decision, Login.tsx inputs keep `outline-none` because the parent `group-focus-within:border-tj-gold` provides visible focus indication. The contrast fix here is about TEXT color (text-gray-500/600 on labels/descriptions), not focus indicators.
  </action>
  <verify>
    After replacement, verify each file:
    - `grep -c "text-gray-500\|text-gray-600"` on each of the 7 files should return 0 (or only placeholder/border instances)
    - Run a final count across ALL customer-facing pages:
      ```
      grep -rn "text-gray-[56]00" pages/*.tsx | grep -v "admin/" | grep -v "placeholder:"
      ```
      Should return 0 results (excluding any legitimate placeholder uses).
    - Run `npm run build` to confirm no errors
  </verify>
  <done>
    All 7 remaining customer-facing pages have zero text-gray-500/600 on readable text. Combined with Task 1 and prior 11-05 work, every customer-facing page now meets WCAG AA 4.5:1 contrast ratio. POLISH-11 contrast requirement fully satisfied.
  </done>
</task>

</tasks>

<verification>
1. grep for text-gray-500 and text-gray-600 across ALL customer-facing page files (excluding admin/) -- should return zero on readable text
2. Specific file counts:
   - Inventory.tsx: 0 instances of text-gray-500/600 on readable text
   - VinLookup.tsx: 0 instances
   - CustomerLogin.tsx: 0 instances
   - RegistrationTracker.tsx: 0 instances
   - NotFound.tsx: 0 instances
   - Legal.tsx: 0 instances
   - Policies.tsx: 0 instances
   - Login.tsx: 0 instances
   - CustomerStatusTracker.tsx: 0 instances
3. `npm run build` passes with zero errors
4. No admin pages were modified (intentionally out of scope)
</verification>

<success_criteria>
- POLISH-11 contrast gap fully closed: zero text-gray-500/600 on readable text across all customer-facing pages
- All body text on dark backgrounds meets WCAG AA 4.5:1 minimum contrast ratio (text-gray-400 = ~5.5:1)
- Build passes with no regressions
- Admin pages intentionally unchanged (out of scope per 11-05 decision)
</success_criteria>

<output>
After completion, create `.planning/phases/11-production-polish/11-08-SUMMARY.md`
</output>
