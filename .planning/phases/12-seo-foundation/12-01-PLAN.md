---
phase: 12-seo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - triple-j-auto-investment-main/App.tsx
  - triple-j-auto-investment-main/components/SEO.tsx
  - triple-j-auto-investment-main/pages/Home.tsx
  - triple-j-auto-investment-main/pages/About.tsx
  - triple-j-auto-investment-main/pages/Inventory.tsx
  - triple-j-auto-investment-main/pages/Contact.tsx
  - triple-j-auto-investment-main/pages/Services.tsx
  - triple-j-auto-investment-main/pages/Finance.tsx
  - triple-j-auto-investment-main/pages/FAQ.tsx
  - triple-j-auto-investment-main/pages/VinLookup.tsx
  - triple-j-auto-investment-main/pages/Policies.tsx
  - triple-j-auto-investment-main/pages/PaymentOptions.tsx
  - triple-j-auto-investment-main/pages/Legal.tsx
  - triple-j-auto-investment-main/pages/NotFound.tsx
  - triple-j-auto-investment-main/pages/Login.tsx
  - triple-j-auto-investment-main/pages/CustomerLogin.tsx
  - triple-j-auto-investment-main/pages/CustomerDashboard.tsx
autonomous: true

must_haves:
  truths:
    - "All public pages use clean URLs without hash fragments (e.g., /about not /#/about)"
    - "Direct URL entry (e.g., typing /about in browser bar) works without 404"
    - "Each public page has a unique, accurate <title> visible in the browser tab"
    - "Each public page has a unique <meta name='description'> in page source"
    - "OG tags on each page reflect that page's content, not a site-wide default"
    - "Private pages (login, customer portal, admin) have noindex meta tags"
    - "Legacy hash URLs (/#/about) redirect to clean URLs (/about)"
  artifacts:
    - path: "triple-j-auto-investment-main/components/SEO.tsx"
      provides: "Reusable SEO meta component using React 19 native <title> and <meta>"
      exports: ["SEO"]
    - path: "triple-j-auto-investment-main/App.tsx"
      provides: "BrowserRouter (not HashRouter) + HashRedirect component"
      contains: "BrowserRouter as Router"
  key_links:
    - from: "triple-j-auto-investment-main/App.tsx"
      to: "react-router-dom"
      via: "BrowserRouter import (not HashRouter)"
      pattern: "BrowserRouter as Router"
    - from: "triple-j-auto-investment-main/pages/*.tsx"
      to: "triple-j-auto-investment-main/components/SEO.tsx"
      via: "import { SEO } from '../components/SEO'"
      pattern: "<SEO"
---

<objective>
Migrate from HashRouter to BrowserRouter for clean SEO-friendly URLs, create a reusable SEO component using React 19 native metadata, and add per-page title/description/OG tags to all 17 page components.

Purpose: Hash URLs (/#/about) are invisible to search engines. This plan makes every public page discoverable with clean URLs and unique, keyword-optimized metadata. Private pages get noindex protection.

Output: BrowserRouter active, SEO.tsx component, all pages rendering per-page metadata, legacy hash URLs redirect to clean paths.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-seo-foundation/12-RESEARCH.md
@.planning/phases/12-seo-foundation/12-CONTEXT.md

Key codebase files:
@triple-j-auto-investment-main/App.tsx (line 3: HashRouter import; line ~617: <Router> usage)
@triple-j-auto-investment-main/pages/Home.tsx
@triple-j-auto-investment-main/pages/About.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: BrowserRouter migration + HashRedirect + SEO component</name>
  <files>
    triple-j-auto-investment-main/App.tsx
    triple-j-auto-investment-main/components/SEO.tsx
  </files>
  <action>
**App.tsx -- BrowserRouter migration (line 3):**

Change the import on line 3 from:
```
import { HashRouter as Router, Routes, Route, Navigate, useLocation, Link, useNavigate } from 'react-router-dom';
```
to:
```
import { BrowserRouter as Router, Routes, Route, Navigate, useLocation, Link, useNavigate } from 'react-router-dom';
```

This is the ONLY change needed for routing. The `as Router` alias means all downstream JSX (`<Router>`) stays identical. No route definitions, Link components, or useNavigate/useLocation calls need to change.

**App.tsx -- HashRedirect component:**

Add a small component inside App.tsx (above AppContent or alongside other utility components) that handles legacy hash URLs:

```typescript
const HashRedirect = () => {
  const navigate = useNavigate();
  useEffect(() => {
    if (window.location.hash.startsWith('#/')) {
      const path = window.location.hash.slice(1); // remove '#', keep '/about'
      navigate(path, { replace: true });
    }
  }, [navigate]);
  return null;
};
```

Render `<HashRedirect />` right after `<ScrollToTop />` inside the Router, so it runs on initial load. This ensures anyone with bookmarked or cached `/#/about` links gets silently redirected to `/about`.

**components/SEO.tsx -- New file:**

Create the reusable SEO component using React 19 native `<title>` and `<meta>` hoisting:

```typescript
interface SEOProps {
  title: string;
  description: string;
  path: string;
  ogTitle?: string;
  ogDescription?: string;
  noindex?: boolean;
}

const SITE_URL = 'https://triplejautoinvestment.com';
const OG_IMAGE = `${SITE_URL}/GoldTripleJLogo.png`;

export const SEO = ({
  title,
  description,
  path,
  ogTitle,
  ogDescription,
  noindex = false,
}: SEOProps) => (
  <>
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={`${SITE_URL}${path}`} />
    <meta property="og:title" content={ogTitle || title} />
    <meta property="og:description" content={ogDescription || description} />
    <meta property="og:url" content={`${SITE_URL}${path}`} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content={OG_IMAGE} />
    {noindex && <meta name="robots" content="noindex, nofollow" />}
  </>
);
```

Key design choices:
- Uses React 19 native `<title>` and `<meta>` (auto-hoisted to `<head>`) -- NOT react-helmet
- Single `<title>` per page (React 19 docs warn against multiple simultaneous titles)
- Canonical URL prevents duplicate content issues between old hash URLs and new clean URLs
- `noindex` prop for private pages
- `og:image` uses the existing GoldTripleJLogo.png (one brand image is appropriate for a small dealer site)
- Do NOT add `itemProp` to any `<meta>` tags (React 19 treats those differently -- renders inline, not hoisted)
  </action>
  <verify>
Run `npm run build` (or the project's build command) from the triple-j-auto-investment-main directory. Build must succeed with zero TypeScript errors. Confirm `BrowserRouter` appears in the built output by searching the dist output, and confirm `HashRouter` does NOT appear.
  </verify>
  <done>
App.tsx imports BrowserRouter (not HashRouter). HashRedirect component exists and renders inside Router. SEO.tsx exports a typed SEO component with title, description, path, OG tags, canonical URL, and noindex support.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SEO metadata to all 17 page components</name>
  <files>
    triple-j-auto-investment-main/pages/Home.tsx
    triple-j-auto-investment-main/pages/About.tsx
    triple-j-auto-investment-main/pages/Inventory.tsx
    triple-j-auto-investment-main/pages/Contact.tsx
    triple-j-auto-investment-main/pages/Services.tsx
    triple-j-auto-investment-main/pages/Finance.tsx
    triple-j-auto-investment-main/pages/FAQ.tsx
    triple-j-auto-investment-main/pages/VinLookup.tsx
    triple-j-auto-investment-main/pages/Policies.tsx
    triple-j-auto-investment-main/pages/PaymentOptions.tsx
    triple-j-auto-investment-main/pages/Legal.tsx
    triple-j-auto-investment-main/pages/NotFound.tsx
    triple-j-auto-investment-main/pages/Login.tsx
    triple-j-auto-investment-main/pages/CustomerLogin.tsx
    triple-j-auto-investment-main/pages/CustomerDashboard.tsx
  </files>
  <action>
Import and add the `<SEO>` component at the top of each page's return JSX (inside a Fragment `<>...</>` wrapper if the page doesn't already have one). Each page wraps its existing content -- do NOT restructure existing JSX.

**Pattern for each page:**
```tsx
import { SEO } from '../components/SEO';
// ... existing imports

const PageName = () => {
  return (
    <>
      <SEO
        title="..."
        description="..."
        path="/..."
      />
      {/* ALL existing page JSX unchanged */}
    </>
  );
};
```

**10 PUBLIC PAGES -- Use these exact titles and descriptions (keyword-optimized per CONTEXT.md targets):**

1. **Home.tsx** -- path: `/`
   - title: `Triple J Auto Investment | Used Cars Houston | Sales & Rentals`
   - description: `Affordable pre-owned vehicles for Houston families. Cars, trucks, and SUVs from $3,000-$8,000. Vehicle rentals available. Se habla espanol. 8774 Almeda Genoa Rd. (832) 400-9760.`

2. **Inventory.tsx** -- path: `/inventory`
   - title: `Used Cars for Sale in Houston | Triple J Auto Investment`
   - description: `Browse affordable used cars, trucks, and SUVs in Houston. Vehicles from $3,000-$8,000 with transparent pricing. Visit us at 8774 Almeda Genoa Rd or call (832) 400-9760.`

3. **About.tsx** -- path: `/about`
   - title: `About Triple J Auto Investment | Houston Pre-Owned Dealer`
   - description: `Family-owned Houston auto dealer at 8774 Almeda Genoa Rd. Affordable pre-owned vehicles and rentals. Serving South Houston, Pasadena, and Pearland. Texas Dealer License P171632.`

4. **Contact.tsx** -- path: `/contact`
   - title: `Contact Triple J Auto Investment | Houston Car Dealer | (832) 400-9760`
   - description: `Visit Triple J Auto Investment at 8774 Almeda Genoa Rd, Houston TX 77075. Call (832) 400-9760. Open Mon-Sat 9AM-6PM. Se habla espanol. Sales and rental inquiries welcome.`

5. **Services.tsx** -- path: `/services`
   - title: `Auto Services & Car Rentals Houston | Triple J Auto Investment`
   - description: `Vehicle sales, car rentals, and automotive services in Houston. Affordable rentals and pre-owned vehicles from $3,000-$8,000. Se habla espanol. (832) 400-9760.`

6. **Finance.tsx** -- path: `/finance`
   - title: `Car Financing Houston | Buy Here Pay Here | Triple J Auto Investment`
   - description: `Flexible auto financing in Houston. Third-party lenders, low down payments. Buy here pay here options. Pre-owned vehicles from $3,000-$8,000. Apply today at Triple J Auto.`

7. **FAQ.tsx** -- path: `/faq`
   - title: `FAQ | Triple J Auto Investment Houston | Common Questions`
   - description: `Answers to common questions about buying used cars at Triple J Auto Investment in Houston. AS-IS sales policy, financing, trade-ins, title transfer, and payment options.`

8. **VinLookup.tsx** -- path: `/vin`
   - title: `Free VIN Check Houston | Triple J Auto Investment`
   - description: `Look up any vehicle by VIN number. Get specifications, history, and details. Free VIN check tool from Triple J Auto Investment, Houston TX.`

9. **Policies.tsx** -- path: `/policies`
   - title: `Dealer Policies | Triple J Auto Investment Houston`
   - description: `Sales policies, return policy, payment terms, and dealer guidelines at Triple J Auto Investment. 8774 Almeda Genoa Rd, Houston TX 77075.`

10. **PaymentOptions.tsx** -- path: `/payment-options`
    - title: `Payment Options | Triple J Auto Investment Houston`
    - description: `Accepted payment methods at Triple J Auto Investment: cash, cashier's check, debit card, and approved financing. Houston TX. (832) 400-9760.`

**1 DYNAMIC PAGE:**

11. **Legal.tsx** -- path: `/legal` (dynamic sections)
    - This page uses `useParams` for `:section`. Build the title dynamically:
    - title: `{sectionTitle} | Triple J Auto Investment` where sectionTitle maps from the URL param (e.g., "privacy" -> "Privacy Policy", "terms" -> "Terms of Service")
    - description: `Legal information and policies for Triple J Auto Investment. Houston TX dealer, License P171632.`
    - path: `/legal/${section}` (use the actual section param)
    - Do NOT add noindex (legal pages can build trust signals for Google)

**4 PRIVATE PAGES (noindex):**

12. **NotFound.tsx** -- path: `/404`
    - title: `Page Not Found | Triple J Auto Investment`
    - description: `The page you're looking for doesn't exist.`
    - noindex: true

13. **Login.tsx** -- path: `/login`
    - title: `Admin Login | Triple J Auto Investment`
    - description: `Admin login portal.`
    - noindex: true

14. **CustomerLogin.tsx** -- path: `/customer/login`
    - title: `Customer Login | Triple J Auto Investment`
    - description: `Customer portal login.`
    - noindex: true

15. **CustomerDashboard.tsx** -- path: `/customer/dashboard`
    - title: `My Dashboard | Triple J Auto Investment`
    - description: `Customer dashboard.`
    - noindex: true

**Do NOT modify these pages** (admin-only, already blocked by robots.txt):
- pages/admin/* (Dashboard, Inventory, Registrations, Rentals, Plates)
- pages/RegistrationTracker.tsx (admin tool)
- pages/CustomerStatusTracker.tsx (token-based private access -- accessed via /track/:accessKey, not a browsable page)

**IMPORTANT implementation notes:**
- Each page needs `import { SEO } from '../components/SEO';` (or `'../../components/SEO'` for nested pages)
- If a page already returns a single root element like `<div>`, wrap it with `<>...<SEO />...<div>...</div></>` (Fragment)
- Do NOT use `document.title = "..."` in useEffect -- that's the old pattern. React 19 native `<title>` handles it declaratively.
- Alias routes (`/vin/free-check` -> VinLookup, `/terms` -> Policies, `/commercial-wholesale` -> Contact) do NOT need separate SEO treatment because they render the same component. The canonical URL in the SEO component handles deduplication.
  </action>
  <verify>
Run `npm run build` from the project directory. Build must succeed with zero errors. Then start the dev server (`npm run dev`) and:
1. Navigate to `/about` -- browser tab should show "About Triple J Auto Investment | Houston Pre-Owned Dealer"
2. Navigate to `/inventory` -- browser tab should show "Used Cars for Sale in Houston | Triple J Auto Investment"
3. Navigate to `/customer/login` -- view page source should contain `<meta name="robots" content="noindex, nofollow">`
4. Navigate to `/` -- browser tab should show "Triple J Auto Investment | Used Cars Houston | Sales & Rentals"
5. Type `/#/about` in the browser address bar -- should redirect to `/about` (clean URL)
  </verify>
  <done>
All 10 public pages render unique, keyword-optimized title tags and meta descriptions. Legal page dynamically builds title from section param. 4 private pages render noindex meta. All pages have canonical URLs and OG tags. Legacy hash URLs redirect cleanly. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. Browser tab shows unique titles on each page (not the same title everywhere)
3. View page source on any public page shows `<meta name="description">` with page-specific content
4. View page source on `/customer/login` shows `<meta name="robots" content="noindex, nofollow">`
5. Direct URL entry (`/about`, `/inventory`, `/finance`) loads correctly without 404
6. Legacy hash URL (`/#/about`) redirects to `/about`
7. OG tags visible in page source with per-page content
8. Canonical URLs present on all pages pointing to `https://triplejautoinvestment.com/...`
</verification>

<success_criteria>
- HashRouter replaced with BrowserRouter -- zero hash fragments in any URL
- SEO.tsx component created and used by all 17 page components
- 10 public pages have unique titles targeting Houston used car keywords
- 10 public pages have unique meta descriptions with business NAP data
- 4 private pages have noindex/nofollow meta tags
- Legal.tsx dynamically generates title from URL section param
- HashRedirect silently converts /#/path bookmarks to /path
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-seo-foundation/12-01-SUMMARY.md`
</output>
