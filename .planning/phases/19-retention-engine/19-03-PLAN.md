---
phase: 19-retention-engine
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - triple-j-auto-investment-main/components/owner/OwnerReferralSection.tsx
  - triple-j-auto-investment-main/components/owner/OwnerUpgradeSection.tsx
  - triple-j-auto-investment-main/pages/ReferralLanding.tsx
  - triple-j-auto-investment-main/pages/OwnerPortal.tsx
  - triple-j-auto-investment-main/App.tsx
  - triple-j-auto-investment-main/components/SEO.tsx
autonomous: true
requirements:
  - PORTAL-04
  - PORTAL-06

must_haves:
  truths:
    - "Each owner has a unique referral code and link they can share via native share or clipboard copy"
    - "The referral section shows personal referral count, tiered reward progress, and community counter"
    - "Referred friends land on /refer/:code and see a warm intro with the referrer's first name before browsing inventory"
    - "Owners who have had their vehicle for 12+ months see an upgrade section with trade-in estimate and matching inventory"
    - "Owners with less than 12 months ownership see a 'not yet' message instead of the upgrade section"
  artifacts:
    - path: "triple-j-auto-investment-main/components/owner/OwnerReferralSection.tsx"
      provides: "Referral code display, share buttons, tier progress, community counter"
      min_lines: 80
    - path: "triple-j-auto-investment-main/components/owner/OwnerUpgradeSection.tsx"
      provides: "Trade-in estimate, inventory matches, upgrade CTA"
      min_lines: 40
    - path: "triple-j-auto-investment-main/pages/ReferralLanding.tsx"
      provides: "Public warm-handoff page for referred friends"
      min_lines: 60
  key_links:
    - from: "triple-j-auto-investment-main/components/owner/OwnerReferralSection.tsx"
      to: "ownerPortalService.ts"
      via: "getCommunityReferralCount()"
      pattern: "getCommunityReferralCount"
    - from: "triple-j-auto-investment-main/pages/ReferralLanding.tsx"
      to: "ownerPortalService.ts"
      via: "getReferrerName() and logReferralClick()"
      pattern: "getReferrerName|logReferralClick"
    - from: "triple-j-auto-investment-main/components/owner/OwnerUpgradeSection.tsx"
      to: "ownerPortalService.ts"
      via: "getUpgradeMatches()"
      pattern: "getUpgradeMatches"
    - from: "triple-j-auto-investment-main/App.tsx"
      to: "ReferralLanding.tsx"
      via: "Route path /refer/:code"
      pattern: "refer/:code"
---

<objective>
Build the Family Circle referral program (code sharing, tier progress, community counter, referral landing page) and the Ready to Upgrade section (trade-in estimate, inventory matches).

Purpose: The referral program turns every buyer into a marketing node. The upgrade section re-enters owners into the sales funnel at 12-18 months post-purchase.
Output: OwnerReferralSection.tsx, OwnerUpgradeSection.tsx components + ReferralLanding.tsx public page + /refer/:code route in App.tsx + wiring into OwnerPortal.tsx.
</objective>

<execution_context>
@C:/Users/jobaw/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jobaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-retention-engine/19-CONTEXT.md
@.planning/phases/19-retention-engine/19-RESEARCH.md
@.planning/phases/19-retention-engine/19-01-SUMMARY.md
@.planning/phases/19-retention-engine/19-02-SUMMARY.md

@triple-j-auto-investment-main/services/ownerPortalService.ts
@triple-j-auto-investment-main/services/marketEstimateService.ts
@triple-j-auto-investment-main/types.ts
@triple-j-auto-investment-main/utils/translations.ts
@triple-j-auto-investment-main/context/Store.tsx
@triple-j-auto-investment-main/App.tsx
@triple-j-auto-investment-main/components/SEO.tsx
@triple-j-auto-investment-main/pages/Inventory.tsx
@triple-j-auto-investment-main/pages/OwnerPortal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OwnerReferralSection and OwnerUpgradeSection components</name>
  <files>
triple-j-auto-investment-main/components/owner/OwnerReferralSection.tsx
triple-j-auto-investment-main/components/owner/OwnerUpgradeSection.tsx
  </files>
  <action>
**OwnerReferralSection.tsx** -- Family Circle referral program:
- Props: { referral: OwnerReferral | null }
- Import getCommunityReferralCount from '../../services/ownerPortalService'.
- Import useLanguage, useState, useEffect.
- Import Share2, Copy, Check, Users, Gift icons from lucide-react.
- Import REFERRAL_TIERS from '../../types'.

If referral is null: show a message like "Your referral code is being generated..." (edge case: trigger hasn't fired yet).

**Referral Code + Link Display:**
- Card section showing referral code in large monospace text (font-mono text-lg tracking-widest text-tj-gold). Example: "MAR-7K2P".
- Below: referral link text in small text-gray-400, truncated with ellipsis.

**Share Buttons (large touch targets per mobile-first decision):**
- "Share Link" button: min-h-[44px] py-4 px-8 text-xs tracking-[0.3em] uppercase. On tap:
  - If navigator.share available AND mobile (check /Mobi|Android/i.test(navigator.userAgent)): use navigator.share({ title: 'Triple J Auto Investment', text: 'I bought my car from Triple J and love it. Check them out!', url: referral.referralLink }).
  - Else: copy referralLink to clipboard via navigator.clipboard.writeText(). Show "Copied!" feedback for 2.5 seconds via state toggle.
  - Use try/catch on both paths (user can cancel share sheet).
- "Copy Code" button: secondary style. Copies referralCode to clipboard. Same copied state feedback.

**Personal Stats:**
- "Your Referrals: X" prominently displayed (text-xl text-white).

**Tier Progress:**
- Show 3 tiers from REFERRAL_TIERS constant.
- For each tier: show tier label, reward amount, and visual indicator (filled circle if reached, empty circle if not). Use referral.referralCount to determine which tiers are reached.
- Visual: small horizontal progress dots or a vertical step list.

**Community Counter:**
- Fetch getCommunityReferralCount() in useEffect on mount.
- Display: "{count} families introduced to Triple J this month" in text-gray-400 with Users icon.

Card styling: p-6 md:p-8 bg-black/40 border border-tj-gold/10 rounded-lg. Section title: t.ownerPortal.referralTitle.

**OwnerUpgradeSection.tsx** -- Ready to Upgrade?
- Props: { registration: Registration }
- Import getUpgradeMatches from '../../services/ownerPortalService'.
- Import estimateMarketValue from '../../services/marketEstimateService'.
- Import useLanguage, useState, useEffect.
- Import TrendingUp, ArrowRight icons from lucide-react.

**12-month gate:**
- Compute monthsOwned from registration.purchaseDate.
- If monthsOwned < 12: show t.ownerPortal.upgradeNotYet message in a subtle card. Do NOT show trade-in or matches.
- If monthsOwned >= 12: show the full upgrade section.

**Trade-In Estimate:**
- Use estimateMarketValue(registration.soldPrice or purchasePrice, registration.vehicleYear, estimated current mileage).
- Estimated current mileage: (registration.mileageAtPurchase || 80000) + (monthsOwned * 1000).
- Display as large gold text: "Estimated Trade-In Value: $X,XXX".

**Matching Inventory:**
- Fetch getUpgradeMatches(registration price) in useEffect.
- Show up to 3 vehicle cards (compact format): year/make/model, price, "View Details" link to /vehicles/:slug.
- If no matches: show "Check back soon -- new inventory arriving weekly" message.

**CTA:**
- "Talk to Us About Upgrading" button linking to /contact. Style: py-4 px-8 text-xs tracking-[0.3em] bg-tj-gold text-black hover:bg-tj-gold/90 rounded. Full width on mobile.

Card styling: same as other sections. Section title: t.ownerPortal.upgradeTitle with TrendingUp icon.
  </action>
  <verify>
Run `npx tsc --noEmit` from triple-j-auto-investment-main directory. Zero new type errors. Verify both files exist in components/owner/. Verify OwnerReferralSection handles null referral gracefully. Verify OwnerUpgradeSection has the 12-month gate.
  </verify>
  <done>OwnerReferralSection displays referral code, share buttons with clipboard fallback, tier progress (3 tiers), personal count, and community counter. OwnerUpgradeSection shows trade-in estimate and matching inventory only after 12 months, with "Talk to Us" CTA. Both compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create ReferralLanding page, wire routes in App.tsx, and integrate sections into OwnerPortal.tsx</name>
  <files>
triple-j-auto-investment-main/pages/ReferralLanding.tsx
triple-j-auto-investment-main/pages/OwnerPortal.tsx
triple-j-auto-investment-main/App.tsx
triple-j-auto-investment-main/components/SEO.tsx
  </files>
  <action>
**ReferralLanding.tsx** -- Public warm-handoff page:
- Import useParams from react-router-dom.
- Import getReferrerName, logReferralClick from '../services/ownerPortalService'.
- Import useLanguage, useStore (for vehicles).
- Import SEO component.
- Import { motion } from 'framer-motion' for entrance animation.

**Data fetch on mount:**
- Extract `code` from useParams<{ code: string }>().
- useEffect: if code exists, call getReferrerName(code) to get referrer's first name. Also call logReferralClick(code, getDeviceType()) to log the click. getDeviceType: check navigator.userAgent for /Mobi|Android/i -> 'mobile', else 'desktop'.
- State: referrerName (string | null), loading (boolean), error (boolean).

**Loading state:**
- While loading: show skeleton with "Someone thinks you'd love Triple J..." and animate-pulse on the name area. Do NOT block the inventory grid on this lookup -- show inventory immediately below.

**Warm intro section (above inventory):**
- If referrerName loaded: show centered section with:
  - Small gold label: t.ownerPortal.referralLanding.intro ("A Personal Recommendation") -- text-[10px] uppercase tracking-[0.3em] text-tj-gold.
  - Heading: "{referrerName} thinks you'd love Triple J" -- font-serif text-2xl md:text-3xl text-white. The name in text-tj-gold.
  - Subtext: t.ownerPortal.referralLanding.subtext -- text-gray-400 max-w-md mx-auto.
  - Subtle divider below.
- If code is invalid (no referrer found): skip the warm intro, just show inventory.

**Inventory grid below:**
- Use the vehicles from useStore (same pattern as Inventory.tsx).
- Filter to show only Available vehicles.
- Render a simplified vehicle card grid (3 cols on lg, 2 on md, 1 on mobile).
- Each card: vehicle image, year/make/model, price, "View Details" link to /vehicles/:slug.
- This is intentionally simpler than the full Inventory page -- no filters, no saved vehicles, just browsing.

**SEO:**
- Title: "[Referrer]'s recommendation | Triple J Auto Investment"
- Description: "Browse trusted pre-owned vehicles recommended by a friend."
- noindex: false (referral links can be indexed for link equity).

**Mobile-first:**
- Warm intro: px-4 py-12 text-center.
- Inventory grid: px-4 md:px-6 pb-16.
- Cards: p-6 md:p-8 bg-black/40 border border-tj-gold/10.
- Full width on mobile, max-w-6xl mx-auto on desktop.

**App.tsx route wiring:**
- Add lazy import for ReferralLanding: `const ReferralLanding = lazyWithErrorHandling(() => import('./pages/ReferralLanding'), 'Referral');`
- Add Route: `<Route path="/refer/:code" element={<ReferralLanding />} />`
- Place this route with other public routes (not inside ProtectedRoute or customer auth). It is fully public.

**SEO.tsx update (if needed):**
- If SEO.tsx only handles noindex as a boolean prop, no changes needed. The ReferralLanding page passes its own SEO props.

**Wire OwnerReferralSection + OwnerUpgradeSection into OwnerPortal.tsx:**
- Read the existing OwnerPortal.tsx created by Plan 02.
- Import OwnerReferralSection and OwnerUpgradeSection in OwnerPortal.tsx.
- Add them after OwnerValueTracker in the stacked layout:
  ```
  <OwnerReferralSection referral={referral} />
  <OwnerUpgradeSection registration={registration} />
  ```
- Add a "I Left a Review" button at the bottom of the portal (or in a small section): calls markReviewCompleted(registration.id). Shows t.ownerPortal.reviewThanks after click. This is the opt-out mechanism for the 7-day follow-up review request.
- Import markReviewCompleted from '../services/ownerPortalService' (add to existing import line).
  </action>
  <verify>
Run `npx tsc --noEmit` from triple-j-auto-investment-main directory. Zero new type errors. Verify App.tsx has both /owner and /refer/:code routes. Verify ReferralLanding.tsx exists and imports getReferrerName + logReferralClick. Verify OwnerPortal.tsx now imports and renders all 6 owner components (VehicleCard, Documents, ServiceReminders, ValueTracker, ReferralSection, UpgradeSection). Run `npx vite build` to confirm build succeeds.
  </verify>
  <done>ReferralLanding.tsx shows warm intro with referrer's first name + simplified inventory grid below. /refer/:code route is public in App.tsx. OwnerPortal.tsx now renders all 6 sections including referral and upgrade. "I Left a Review" button exists in portal. Build compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npx vite build` succeeds
3. App.tsx has /refer/:code route (public, no auth)
4. ReferralLanding.tsx fetches referrer name and logs click
5. OwnerReferralSection has navigator.share + clipboard fallback
6. OwnerUpgradeSection gates on 12 months ownership
7. OwnerPortal.tsx renders all 6 section components
8. All text uses t.ownerPortal.* bilingual keys
</verification>

<success_criteria>
- Referral code and link are displayed with share/copy buttons that work on both mobile (native share) and desktop (clipboard)
- Tier progress shows 3 reward levels with visual indicators
- Community counter shows monthly referral click count
- /refer/:code shows warm intro with referrer's first name, then inventory grid
- Invalid referral codes gracefully skip the warm intro
- Upgrade section only appears after 12+ months ownership
- Upgrade section shows trade-in estimate and up to 3 matching vehicles
- "I Left a Review" button marks review as completed in DB
</success_criteria>

<output>
After completion, create `.planning/phases/19-retention-engine/19-03-SUMMARY.md`
</output>
