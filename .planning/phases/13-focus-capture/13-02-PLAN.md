---
phase: 13-focus-capture
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - triple-j-auto-investment-main/pages/Home.tsx
autonomous: true

must_haves:
  truths:
    - "Hero section shows abstract animated SVG background with gold-accented flowing geometric shapes on dark background"
    - "Hero displays 'TRIPLE J AUTO INVESTMENT' heading and 'Automotive Investment Firm' positioning language from translations"
    - "Hero CTAs are 'Schedule a Visit' (primary) and 'Call Now' (secondary) using canonical button pattern"
    - "Se Habla Espanol badge is visible in the hero area"
    - "Hero extends full-bleed behind transparent navbar for immersive visual impact"
    - "Authority metrics section shows animated count-up numbers when scrolled into view"
    - "Authority metrics display at least 3 real business metrics with labels from translations"
    - "All visible text comes from translation keys (no hardcoded English)"
    - "SVG animation has fewer than 12 animated paths for mobile performance"
    - "Sections below the hero (ticker, featured vehicles, value pillars, etc.) remain unchanged"
  artifacts:
    - path: "triple-j-auto-investment-main/pages/Home.tsx"
      provides: "New hero section with abstract SVG animation, authority metrics with count-up"
      min_lines: 400
  key_links:
    - from: "triple-j-auto-investment-main/pages/Home.tsx"
      to: "triple-j-auto-investment-main/utils/translations.ts"
      via: "t.home.hero.heading, t.home.hero.subheading, t.home.hero.scheduleVisit, t.home.authority.*"
      pattern: "t\\.home\\.hero\\.|t\\.home\\.authority\\."
    - from: "triple-j-auto-investment-main/pages/Home.tsx"
      to: "framer-motion"
      via: "motion.path for SVG animation, useInView + useSpring for count-up"
      pattern: "useInView|motion\\.path|useSpring"
    - from: "triple-j-auto-investment-main/pages/Home.tsx"
      to: "tel:+18324009760"
      via: "Call Now CTA href"
      pattern: "tel:\\+18324009760"
---

<objective>
Replace the current stock-photo hero section in Home.tsx with an abstract animated SVG hero and add an authority metrics strip section with animated count-up numbers.

Purpose: Break the "used car lot" expectation in the visitor's first 3 seconds. The abstract animation signals competence and intentionality. Authority metrics build instant trust. Together they reframe Triple J from a commodity used car lot to a premium automotive investment firm.

Output: Home.tsx with new hero (abstract SVG animation + positioning language + CTAs + Se Habla badge) and new authority metrics section (count-up numbers). All sections below remain unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-focus-capture/13-RESEARCH.md
@.planning/phases/13-focus-capture/13-01-SUMMARY.md

Key source files:
@triple-j-auto-investment-main/pages/Home.tsx
@triple-j-auto-investment-main/utils/translations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hero section with abstract SVG animation and new content</name>
  <files>triple-j-auto-investment-main/pages/Home.tsx</files>
  <action>
Replace the ENTIRE hero section (lines 106-190 in current Home.tsx) and remove the DecryptText component (lines 11-42) and mouse parallax state/effects (lines 47, 55-56, 66-79). Replace with:

**1. Update imports (line 5):**
Replace `import { motion, useScroll, useTransform, useSpring } from 'framer-motion';` with:
`import { motion, useScroll, useTransform, useSpring, useInView, useMotionValue } from 'framer-motion';`

Also add `useRef` to the React import if not already there.

Remove `Diamond` from lucide imports if no longer used (check cards section -- Diamond IS used in cards, keep it). Remove `ChevronDown` ONLY if the new scroll indicator does not use it (likely still needed). Keep `Phone` (used in CTA).

**2. Remove DecryptText component** (lines 11-42 entirely). It is not reused anywhere else.

**3. Remove mouse parallax state** -- delete `const [mousePos, setMousePos] = useState({ x: 0, y: 0 })` (line 47), `const mouseX = useSpring(...)` and `const mouseY = useSpring(...)` (lines 55-56), and the entire `useEffect` with `handleMouseMove` (lines 66-79).

**4. New hero section structure** (replaces lines 106-190):

The hero should be a full-bleed section that extends behind the navbar. Use `min-h-screen -mt-36 pt-36` to achieve this (negative margin pulls it up behind the fixed navbar's padding zone, then pt-36 compensates for content positioning).

```
Hero container: relative min-h-screen -mt-36 flex flex-col justify-center items-center overflow-hidden

  Background layer (absolute inset-0, pointer-events-none):
    - SVG element: absolute inset-0, w-full h-full, viewBox="0 0 1000 700", preserveAspectRatio="xMidYMid slice"
    - <defs> with linearGradient for gold paths:
      - id="heroGoldPath": stops at 0% rgba(212,175,55,0), 50% rgba(212,175,55,0.3), 100% rgba(212,175,55,0)
      - id="heroGoldGlow": stops at 0% rgba(212,175,55,0), 40% rgba(212,175,55,0.15), 60% rgba(212,175,55,0.15), 100% rgba(212,175,55,0)
    - 8-10 animated motion.path elements (UNDER 12 for mobile performance):
      - Each with unique bezier curve d attribute (flowing, organic curves -- not straight geometric lines)
      - stroke="url(#heroGoldPath)", strokeWidth varies 0.5-2, fill="none"
      - Animate pathLength: [0, 1, 0] and opacity: [0, 0.4-0.7, 0]
      - duration: 5-8 seconds each (staggered), repeat: Infinity, ease: "easeInOut"
      - delay: stagger each by 0.3-0.5s
    - 3-4 animated motion.circle elements (floating particles):
      - Small circles (r=1-2) with gold fill at low opacity (0.15-0.3)
      - Animate cy (vertical float) and opacity
      - Slow, continuous loop (8-12s duration)
    - Subtle radial gradient overlay: absolute inset-0, bg-[radial-gradient(ellipse_at_center,transparent_0%,rgba(0,0,0,0.4)_70%,rgba(0,0,0,0.8)_100%)]

  Content layer (relative z-10, text-center, px-6, max-w-4xl mx-auto):
    - "Se Habla Espanol" badge at top:
      - Framer Motion entrance: initial opacity:0 y:-20, animate opacity:1 y:0, delay 0.3
      - Styled as small badge: inline-flex items-center gap-2, border border-tj-gold/30 rounded-full px-4 py-1.5 mb-8
      - Gold text: text-tj-gold text-[10px] uppercase tracking-[0.3em] font-bold
      - Small globe icon or accent dot before text
      - Text from: {t.home.seHabla}

    - Main heading (h1):
      - Framer Motion entrance: initial opacity:0 y:40, animate opacity:1 y:0, delay 0.5, spring type
      - font-display (Cinzel), text-4xl md:text-6xl lg:text-7xl, text-white, tracking-tight, mb-4
      - Text from: {t.home.hero.heading}

    - Subheading:
      - Framer Motion entrance: initial opacity:0 y:30, animate opacity:1 y:0, delay 0.7
      - text-tj-gold, text-xs md:text-sm, uppercase, tracking-[0.3em], font-bold, mb-6
      - Text from: {t.home.hero.subheading}

    - Tagline:
      - Framer Motion entrance: initial opacity:0, animate opacity:1, delay 0.9
      - font-serif italic, text-sm md:text-lg, text-gray-400, max-w-xl mx-auto, mb-10
      - Text from: {t.home.hero.tagline}

    - CTA buttons row:
      - Framer Motion entrance: initial opacity:0 y:30, animate opacity:1 y:0, delay 1.1
      - flex flex-col sm:flex-row items-center gap-4

      Primary CTA -- "Schedule a Visit":
        - <Link to="/contact"> (contact page has the visit scheduling form)
        - Canonical pattern: bg-tj-gold text-black py-4 px-8 text-xs font-bold tracking-[0.3em] uppercase
        - Hover: hover:bg-white transition-colors duration-300
        - Text from: {t.home.hero.scheduleVisit}

      Secondary CTA -- "Call Now":
        - <a href="tel:+18324009760">
        - Canonical pattern: bg-transparent border-2 border-white text-white py-4 px-8 text-xs font-bold tracking-[0.3em] uppercase
        - Hover: hover:bg-tj-gold hover:border-tj-gold hover:text-black transition-all duration-300
        - Phone icon: <Phone size={14} /> before text
        - Text from: {t.home.hero.callNow}

  Scroll indicator (absolute bottom-8, z-10):
    - Framer Motion entrance: initial opacity:0, animate opacity:1, delay 2, duration 1
    - animate-bounce, text-tj-gold/50
    - Text from: {t.home.hero.scrollPrompt}
    - ChevronDown icon below
    - onClick: scroll to first section below hero

**Path design guidance for the abstract SVG animation:**
Create flowing, organic bezier curves that evoke motion and aspiration. The paths should:
- Start from off-screen (negative x values) and flow across the viewport
- Have gentle vertical undulation (not flat horizontal lines)
- Vary in vertical position to fill the viewport height
- Example path: "M -100 250 Q 200 100 500 300 T 1100 200"
- Another: "M -50 450 C 200 350 400 500 600 400 S 900 300 1100 450"
- Mix of wider sweeping curves and tighter arcs

**CRITICAL things to preserve in Home.tsx:**
- The `useScroll` / `useTransform` for y1/opacity can be REMOVED (was for the old parallax). Or repurpose for a subtle hero content scroll fade.
- Keep ALL code below the hero section (ticker at ~line 192, featured vehicles, value pillars, dealership info, what sets us apart, inventory CTA) completely unchanged.
- Keep the SEO component at the top unchanged.
- Keep the outer motion.div wrapper unchanged.
- Keep the `containerVariants` and `itemVariants` (used by featured vehicles section).
- Keep `featuredVehicles` logic and `hasVehicles` (used below hero).

**Performance considerations:**
- All SVG paths use motion.path from framer-motion (hardware-accelerated)
- No useState for mouse position (removed). If any interactivity desired, use useMotionValue instead
- Keep total animated SVG elements under 12 (8-10 paths + 3-4 circles = ~12 max)
- Add will-change: auto (NOT will-change: transform on everything -- let the browser decide)
  </action>
  <verify>
Run `npm run build` from the triple-j-auto-investment-main directory. Build succeeds with no errors. Then visually check:
1. Open the dev server (`npm run dev`) and load the homepage
2. Hero shows abstract gold animated paths on dark background
3. "Se Habla Espanol" badge visible in hero
4. Heading says "TRIPLE J AUTO INVESTMENT"
5. Subheading says "AUTOMOTIVE INVESTMENT FIRM" (or Spanish equivalent)
6. CTAs: "Schedule a Visit" links to /contact, "Call Now" links to tel:
7. Scroll down: ticker, featured vehicles, etc. all render unchanged
  </verify>
  <done>
Home.tsx hero section replaced with abstract SVG animation background, positioning language heading/subheading, "Se Habla" badge, and Schedule a Visit / Call Now CTAs. All text from translation system. DecryptText component removed. Mouse parallax state removed. All sections below hero unchanged. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add authority metrics strip section below hero</name>
  <files>triple-j-auto-investment-main/pages/Home.tsx</files>
  <action>
Add a new authority metrics section BETWEEN the hero section and the existing ticker section in Home.tsx. This section shows animated count-up numbers that trigger when scrolled into view.

**Section structure:**

```
{/* --- AUTHORITY METRICS --- */}
<section className="relative py-16 md:py-24 bg-black border-b border-white/10 overflow-hidden">
  {/* Subtle gradient background accent */}
  <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_top,rgba(212,175,55,0.03)_0%,transparent_60%)]" />

  <div className="relative max-w-5xl mx-auto px-4 md:px-6">
    {/* Section title (optional -- Claude's discretion on whether to include) */}
    <p className="text-center text-[10px] uppercase tracking-[0.4em] text-tj-gold/60 mb-12 font-display">
      {t.home.authority.title}
    </p>

    {/* Metrics grid */}
    <div className="grid grid-cols-2 md:grid-cols-4 gap-8 md:gap-12">
      {metrics.map((metric, i) => (
        <CountUpMetric key={i} ... />
      ))}
    </div>
  </div>
</section>
```

**CountUpMetric inline component** (define above the Home component or inside it):

Use `useRef`, `useInView`, `useSpring`, and `useTransform` from framer-motion. Pattern from research:

```tsx
const CountUpNumber = ({ value, suffix = '', label }: { value: number; suffix?: string; label: string }) => {
  const ref = useRef<HTMLDivElement>(null);
  const isInView = useInView(ref, { once: true, margin: "-100px" });
  const spring = useSpring(0, { mass: 0.8, stiffness: 75, damping: 15 });
  const display = useTransform(spring, (current) => Math.round(current).toLocaleString());

  useEffect(() => {
    if (isInView) {
      spring.set(value);
    }
  }, [isInView, spring, value]);

  return (
    <div ref={ref} className="text-center">
      <div className="text-3xl md:text-5xl font-display text-white mb-2 tabular-nums">
        <motion.span>{display}</motion.span>
        {suffix && <span className="text-tj-gold">{suffix}</span>}
      </div>
      <p className="text-[10px] md:text-xs uppercase tracking-[0.2em] text-gray-400">{label}</p>
    </div>
  );
};
```

**Metrics data** (define as a constant array using translation keys):

```tsx
const metrics = [
  { value: 500, suffix: '+', label: t.home.authority.familiesServed },
  { value: 150, suffix: '+', label: t.home.authority.fiveStarReviews },
  { value: 3, suffix: '+', label: t.home.authority.yearsInBusiness },
  { value: 800, suffix: '+', label: t.home.authority.vehiclesDelivered },
];
```

NOTE: These numbers are PLACEHOLDER estimates. Add a code comment above the metrics array:
```tsx
// Authority metrics -- update these numbers with real business data
// These are conservative estimates; replace with actual figures when available
```

**Visual styling details:**
- Numbers: font-display (Cinzel), text-3xl md:text-5xl, text-white
- Suffix (+): text-tj-gold (gold accent on the plus sign)
- Labels: text-[10px] md:text-xs, uppercase tracking-[0.2em], text-gray-400
- Grid: 2 columns on mobile (2x2 grid), 4 columns on desktop (single row)
- Each metric has a subtle entrance animation: motion.div with initial opacity:0 y:20, whileInView opacity:1 y:0, staggered by 0.1s delay per item
- Section uses standard spacing: py-16 md:py-24

**Section placement:** Insert this section AFTER the closing `</div>` of the hero section and BEFORE the `{/* --- TICKER --- */}` comment.

**Separation between hero and metrics:**
- No parallax transition between them (clean section break per Context decisions -- Claude's discretion, but clean break is simpler and more reliable)
- The hero's dark background naturally transitions to the metrics section's black background
- The border-b border-white/10 on the metrics section creates a subtle divider before the ticker
  </action>
  <verify>
Run `npm run build` from the triple-j-auto-investment-main directory. Build succeeds. Then visually check:
1. Scroll down from hero -- authority metrics section appears
2. Four metrics visible in a grid (2x2 on mobile, 4 across on desktop)
3. Numbers animate from 0 to target value when scrolled into view
4. Numbers only animate ONCE (not on every scroll)
5. Labels show translated text (English or Spanish based on language toggle)
6. Ticker section still renders correctly below the metrics
  </verify>
  <done>
Authority metrics strip section added between hero and ticker in Home.tsx. Four count-up metrics (families served, five-star reviews, years in business, vehicles delivered) animate from 0 to target when scrolled into view. Responsive 2-column mobile / 4-column desktop grid. All labels from translation system. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. Homepage loads with new abstract SVG animated hero (no stock photo)
3. Hero text shows positioning language from translations (both EN and ES)
4. "Se Habla Espanol" badge visible in hero
5. CTAs: "Schedule a Visit" -> /contact, "Call Now" -> tel:+18324009760
6. Authority metrics section visible below hero with animated count-up numbers
7. Language toggle switches all hero + metrics text between English and Spanish
8. All sections below metrics (ticker, featured vehicles, pillars, etc.) render unchanged
9. No console errors
10. SVG animation is smooth (no jank), fewer than 12 animated elements
</verification>

<success_criteria>
- Stock photo hero completely replaced with abstract SVG animation
- All hero text comes from t.home.hero.* translation keys
- Schedule a Visit is primary CTA (links to /contact), Call Now is secondary (tel: link)
- Se Habla Espanol badge visible in hero area
- Authority metrics section shows 4 count-up numbers with translated labels
- Count-up triggers once on scroll into view
- All homepage sections below metrics are unchanged
- Build passes, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-focus-capture/13-02-SUMMARY.md`
</output>
