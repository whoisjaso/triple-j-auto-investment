---
phase: 09-production-infrastructure
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - "triple-j-auto-investment-main/.env.production"
  - "triple-j-auto-investment-main/.env.local"
  - "triple-j-auto-investment-main/.gitignore"
  - "triple-j-auto-investment-main/dokploy.json"
  - "triple-j-auto-investment-main/DEBUG.html"
  - "triple-j-auto-investment-main/DEPLOYMENT.md"
  - "triple-j-auto-investment-main/DOKPLOY_ENV_SETUP.md"
  - "triple-j-auto-investment-main/FORCE_REBUILD.md"
  - "triple-j-auto-investment-main/COMPLETE_SUPABASE_FIX.md"
  - "triple-j-auto-investment-main/SETUP_GUIDE.md"
  - "triple-j-auto-investment-main/FINAL_SUMMARY.md"
  - "triple-j-auto-investment-main/Dockerfile"
  - "triple-j-auto-investment-main/docker-compose.yml"
  - "triple-j-auto-investment-main/nginx.conf"
  - "triple-j-auto-investment-main/scripts/run-schema.js"
  - "triple-j-auto-investment-main/scripts/setup-database.js"
  - "triple-j-auto-investment-main/scripts/add-columns-direct.js"
  - "triple-j-auto-investment-main/scripts/execute-schema-direct.js"
  - "triple-j-auto-investment-main/scripts/add-schema-columns.js"
  - "triple-j-auto-investment-main/scripts/fix-database.js"
  - "triple-j-auto-investment-main/scripts/fix-rls-policies.js"
  - "triple-j-auto-investment-main/scripts/generate-env.sh"
  - "triple-j-auto-investment-main/services/emailService.ts"
  - "triple-j-auto-investment-main/index.html"
  - "triple-j-auto-investment-main/business-plan/README.md"
autonomous: true

must_haves:
  truths:
    - "No hardcoded credentials remain in any tracked file in the repository"
    - "No .env files are tracked by git (all gitignored)"
    - "Deprecated deployment files (Docker, Dokploy) are removed"
    - "Dev scripts with hardcoded credentials are removed"
    - ".gitignore properly excludes .env.local and .env.production"
  artifacts:
    - path: "triple-j-auto-investment-main/.gitignore"
      provides: "Updated gitignore covering all .env files"
      contains: ".env.production"
    - path: "triple-j-auto-investment-main/.env.production"
      provides: "Deleted or emptied -- no longer tracked"
  key_links:
    - from: ".gitignore"
      to: ".env files"
      via: "Git ignore rules prevent env files from being committed"
      pattern: "\\.env"
---

<objective>
Remove all hardcoded credentials from the codebase and delete deprecated deployment files.

Purpose: The research identified 48 files containing hardcoded credentials (Supabase keys, admin passwords, service role JWTs, database connection strings). These must be cleaned before any production credentials are created or committed. The repo is now private (Plan 01), so this cleanup prevents future exposure. Deprecated Docker/Dokploy files that also contain credentials are removed entirely.

Output: Clean codebase with zero hardcoded credentials. All env files gitignored. Deprecated files removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-production-infrastructure/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete deprecated files and dev scripts with credentials</name>
  <files>
    triple-j-auto-investment-main/Dockerfile
    triple-j-auto-investment-main/docker-compose.yml
    triple-j-auto-investment-main/nginx.conf
    triple-j-auto-investment-main/dokploy.json
    triple-j-auto-investment-main/DEBUG.html
    triple-j-auto-investment-main/DOKPLOY_ENV_SETUP.md
    triple-j-auto-investment-main/FORCE_REBUILD.md
    triple-j-auto-investment-main/COMPLETE_SUPABASE_FIX.md
    triple-j-auto-investment-main/scripts/run-schema.js
    triple-j-auto-investment-main/scripts/setup-database.js
    triple-j-auto-investment-main/scripts/add-columns-direct.js
    triple-j-auto-investment-main/scripts/execute-schema-direct.js
    triple-j-auto-investment-main/scripts/add-schema-columns.js
    triple-j-auto-investment-main/scripts/fix-database.js
    triple-j-auto-investment-main/scripts/fix-rls-policies.js
    triple-j-auto-investment-main/scripts/generate-env.sh
    triple-j-auto-investment-main/scripts/migrate-localStorage-to-supabase.ts
  </files>
  <action>
    Delete all of the following files. These are either deprecated deployment configs (Docker/Dokploy -- now using Vercel) or dev utility scripts that contain hardcoded Supabase URLs, service role keys, and database passwords.

    **Deprecated deployment files to DELETE:**
    - `Dockerfile` -- Not needed for Vercel
    - `docker-compose.yml` -- Not needed for Vercel
    - `nginx.conf` -- Not needed for Vercel
    - `dokploy.json` -- Contains hardcoded credentials, Dokploy no longer used
    - `DEBUG.html` -- Contains hardcoded dev Supabase URL and anon key
    - `DOKPLOY_ENV_SETUP.md` -- Contains hardcoded credentials
    - `FORCE_REBUILD.md` -- Docker-specific, contains hardcoded credentials
    - `COMPLETE_SUPABASE_FIX.md` -- Contains hardcoded admin password

    **Dev scripts to DELETE (all contain hardcoded dev credentials):**
    - `scripts/run-schema.js` -- Hardcoded service role key
    - `scripts/setup-database.js` -- Hardcoded service role key + admin password
    - `scripts/add-columns-direct.js` -- Hardcoded database connection string with password
    - `scripts/execute-schema-direct.js` -- Hardcoded Supabase URL
    - `scripts/add-schema-columns.js` -- Hardcoded Supabase URL
    - `scripts/fix-database.js` -- Hardcoded Supabase URL + admin email
    - `scripts/fix-rls-policies.js` -- Hardcoded Supabase URL + anon key
    - `scripts/generate-env.sh` -- Dev env generation script
    - `scripts/migrate-localStorage-to-supabase.ts` -- Dev migration utility

    After deleting all scripts, remove the empty `scripts/` directory if it is empty.

    Use `git rm` for all tracked files so git records the deletion.
  </action>
  <verify>
    Run: `git status` in the `triple-j-auto-investment-main` directory
    - All listed files should show as "deleted" in staging
    - The `scripts/` directory should be empty or removed
    - No other files should be modified
  </verify>
  <done>All deprecated deployment files and dev scripts with hardcoded credentials are deleted from the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Clean remaining credential references and fix .gitignore</name>
  <files>
    triple-j-auto-investment-main/.env.production
    triple-j-auto-investment-main/.env.local
    triple-j-auto-investment-main/.gitignore
    triple-j-auto-investment-main/DEPLOYMENT.md
    triple-j-auto-investment-main/SETUP_GUIDE.md
    triple-j-auto-investment-main/FINAL_SUMMARY.md
    triple-j-auto-investment-main/services/emailService.ts
    triple-j-auto-investment-main/index.html
    triple-j-auto-investment-main/business-plan/README.md
  </files>
  <action>
    **Fix .gitignore to exclude ALL .env files:**
    Update `.gitignore` to add `.env.production` to the ignored list. Change the env section to:
    ```
    # Environment variables - NEVER commit
    .env
    .env.*
    .env.local
    .env.production
    .env.development.local
    .env.test.local
    .env.production.local
    ```
    Remove the old comment that said `.env.production` was committed intentionally.

    **Remove .env.production from git tracking (but keep file locally):**
    Run: `git rm --cached .env.production` (removes from git without deleting file)
    Then replace its contents with a template (no real values):
    ```
    # Production environment variables
    # Set these in Vercel Dashboard -> Settings -> Environment Variables
    # DO NOT commit real values to this file
    VITE_SUPABASE_URL=
    VITE_SUPABASE_ANON_KEY=
    VITE_GEMINI_API_KEY=
    VITE_RETELL_API_KEY=
    VITE_RETELL_OUTBOUND_AGENT_ID=
    VITE_RETELL_OUTBOUND_NUMBER=
    ```

    **Remove .env.local from git tracking if tracked:**
    Run: `git rm --cached .env.local` (if it exists in git)

    **Clean DEPLOYMENT.md:**
    Find and replace all hardcoded credential values with placeholder text:
    - Replace actual Supabase URLs with `https://YOUR_PROJECT.supabase.co`
    - Replace actual anon keys with `YOUR_ANON_KEY`
    - Replace `jobawems@gmail.com` references with `YOUR_ADMIN_EMAIL`
    - Replace `adekunle12` with `YOUR_ADMIN_PASSWORD`
    - Add a note at the top: "NOTE: All credentials are managed through Vercel environment variables and Supabase Dashboard. Never commit real values."

    **Clean SETUP_GUIDE.md:**
    - Replace `adekunle12` with `YOUR_ADMIN_PASSWORD`
    - Replace any hardcoded URLs/keys with placeholders

    **Clean FINAL_SUMMARY.md:**
    - Replace `adekunle12` with `YOUR_ADMIN_PASSWORD`
    - Replace any hardcoded credentials with placeholders

    **Clean services/emailService.ts:**
    - Line 38 has hardcoded `jobawems@gmail.com` as a secondary email. This is a business contact email (not a secret), but review whether it should be configurable. If it's the "to" address for lead notifications, leave it (it's a business decision, not a credential). If it's exposed client-side inappropriately, add a TODO comment.

    **Clean index.html:**
    - Line 62 has `jobawems@gmail.com` in structured data (schema.org). This is legitimate for SEO/business listing -- leave it. This is public business contact info, not a credential.

    **Clean business-plan/README.md:**
    - Line 113 has `jobawems@gmail.com`. This is in a business plan document -- the email itself is public business contact info, not a credential. Leave it.

    **After all changes, run a comprehensive credential scan:**
    Search the entire codebase for remaining credential patterns:
    ```
    grep -r "adekunle12" triple-j-auto-investment-main/ --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" --include="*.html" --include="*.md" --include="*.env*"
    grep -r "scgmpliwlfabnpygvbsy" triple-j-auto-investment-main/ --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" --include="*.html" --include="*.md"
    grep -r "key_86a41f8fad555c03e879a71d0398" triple-j-auto-investment-main/ --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" --include="*.html" --include="*.md" --include="*.env*"
    grep -r "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9" triple-j-auto-investment-main/ --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" --include="*.html" --include="*.md"
    ```

    Each grep should return ZERO results. If any match, clean that file too.

    **Important:** Do NOT modify `supabase/config.ts` -- it reads from `import.meta.env.VITE_SUPABASE_URL` which is the correct pattern. The env vars are provided by Vercel at build time.
  </action>
  <verify>
    - `git status` shows .env.production removed from tracking
    - `grep -r "adekunle12" triple-j-auto-investment-main/` returns nothing (excluding node_modules, .git)
    - `grep -r "scgmpliwlfabnpygvbsy" triple-j-auto-investment-main/` returns nothing (excluding node_modules, .git)
    - `grep -r "key_86a41f8fad555c03e879a71d0398" triple-j-auto-investment-main/` returns nothing (excluding node_modules, .git)
    - `.gitignore` includes `.env.production` and `.env.*`
  </verify>
  <done>Zero hardcoded credentials remain in any tracked file. All .env files are gitignored. Credential scan returns no matches.</done>
</task>

</tasks>

<verification>
Run a final comprehensive scan for any credential patterns:
```bash
cd triple-j-auto-investment-main
# Admin password
grep -r "adekunle12" . --exclude-dir=node_modules --exclude-dir=.git
# Dev Supabase project ref
grep -r "scgmpliwlfabnpygvbsy" . --exclude-dir=node_modules --exclude-dir=.git
# Retell API key
grep -r "key_86a41f8fad" . --exclude-dir=node_modules --exclude-dir=.git
# Service role JWT prefix
grep -r "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9" . --exclude-dir=node_modules --exclude-dir=.git
# DB connection string
grep -r "pooler.supabase.com" . --exclude-dir=node_modules --exclude-dir=.git
```
ALL should return zero results.
</verification>

<success_criteria>
1. All deprecated files (Docker, Dokploy, DEBUG.html) deleted
2. All dev scripts with hardcoded credentials deleted
3. .env.production removed from git tracking
4. .gitignore updated to exclude all .env files
5. DEPLOYMENT.md, SETUP_GUIDE.md, FINAL_SUMMARY.md cleaned of real credentials
6. Comprehensive credential scan returns zero matches for all known credential patterns
</success_criteria>

<output>
After completion, create `.planning/phases/09-production-infrastructure/09-04-SUMMARY.md`
</output>
