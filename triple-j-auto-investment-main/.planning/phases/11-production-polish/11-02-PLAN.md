---
phase: 11-production-polish
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - triple-j-auto-investment-main/pages/Inventory.tsx
  - triple-j-auto-investment-main/pages/Home.tsx
  - triple-j-auto-investment-main/pages/Contact.tsx
  - triple-j-auto-investment-main/pages/Finance.tsx
  - triple-j-auto-investment-main/pages/CustomerDashboard.tsx
  - triple-j-auto-investment-main/pages/CustomerLogin.tsx
  - triple-j-auto-investment-main/pages/About.tsx
  - triple-j-auto-investment-main/pages/VinLookup.tsx
autonomous: true

must_haves:
  truths:
    - "When Supabase is unreachable, the Inventory page shows a clear error message with retry option instead of infinite loading or 'Connection Issue Detected' for empty inventory"
    - "When the inventory is genuinely empty (zero vehicles, no error), the user sees an empty state with a message and CTA, not the connection error state"
    - "When a customer has zero registrations, the CustomerDashboard shows an empty state message instead of blank content"
    - "Contact and Finance form submissions show error UI when the submission fails (not just console.error)"
    - "All loading indicators are visible and branded (no blank screens during async operations)"
    - "All new error/empty/loading text is bilingual (English and Spanish)"
  artifacts:
    - path: "triple-j-auto-investment-main/pages/Inventory.tsx"
      provides: "Separate empty state vs error state for vehicle grid"
      contains: "emptyInventory|connectionError"
    - path: "triple-j-auto-investment-main/pages/CustomerDashboard.tsx"
      provides: "Empty state when customer has no registrations"
      contains: "emptyDashboard"
    - path: "triple-j-auto-investment-main/pages/Contact.tsx"
      provides: "Error state for form submission failure"
      contains: "errorFormSubmit|error"
    - path: "triple-j-auto-investment-main/pages/Finance.tsx"
      provides: "Error state for form submission failure"
      contains: "errorFormSubmit|error"
  key_links:
    - from: "triple-j-auto-investment-main/pages/Inventory.tsx"
      to: "triple-j-auto-investment-main/utils/translations.ts"
      via: "t.polish.emptyInventory and t.polish.errorLoadFailed"
      pattern: "t\\.polish\\."
    - from: "triple-j-auto-investment-main/pages/CustomerDashboard.tsx"
      to: "triple-j-auto-investment-main/utils/translations.ts"
      via: "t.polish.emptyDashboard"
      pattern: "t\\.polish\\.emptyDashboard"
---

<objective>
Add proper loading, empty, and error states to all customer-facing pages that have async operations or data dependencies. Fixes the conflated empty/error states in Inventory, adds missing empty states to CustomerDashboard, and adds error handling to Contact and Finance form submissions.

Purpose: Users must never see a blank screen, infinite spinner, or misleading error message. Every async operation needs three visible states: loading, success, error. Every data-dependent area needs an empty state for zero-data scenarios.

Output: 8 page files updated with proper state handling, all using bilingual translation keys from Plan 01.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-production-polish/11-CONTEXT.md
@.planning/phases/11-production-polish/11-RESEARCH.md
@.planning/phases/11-production-polish/11-01-SUMMARY.md

@triple-j-auto-investment-main/pages/Inventory.tsx
@triple-j-auto-investment-main/pages/Home.tsx
@triple-j-auto-investment-main/pages/Contact.tsx
@triple-j-auto-investment-main/pages/Finance.tsx
@triple-j-auto-investment-main/pages/CustomerDashboard.tsx
@triple-j-auto-investment-main/pages/CustomerLogin.tsx
@triple-j-auto-investment-main/pages/About.tsx
@triple-j-auto-investment-main/pages/VinLookup.tsx
@triple-j-auto-investment-main/utils/translations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Inventory empty/error state separation + Home featured vehicles empty state</name>
  <files>
    triple-j-auto-investment-main/pages/Inventory.tsx
    triple-j-auto-investment-main/pages/Home.tsx
  </files>
  <action>
    **Inventory.tsx -- Separate empty state from error state:**

    The current Inventory page conflates "no vehicles" with "connection error." When `sortedVehicles.length === 0`, it shows a "Connection Issue Detected" troubleshooting guide. This is wrong when the inventory is genuinely empty.

    Read the current Inventory.tsx to understand the existing state management (it uses Store context for vehicles and has loading/connectionError states).

    Fix by implementing THREE distinct states for the vehicle grid area:

    1. **Loading state** (vehicles are being fetched): Show a skeleton grid of 3-6 vehicle card placeholders using Tailwind `animate-pulse` on `bg-gray-800` blocks. Each skeleton should match the vehicle card layout (image area aspect-[4/3], text lines below). Use the existing loading state from Store context.

    2. **Error state** (connectionError is truthy OR fetch failed): Show a clear error message with:
       - Icon or visual indicator (use an existing Lucide icon like `AlertTriangle` or `WifiOff`)
       - Bilingual text: `t.polish.errorLoadFailed`
       - Retry button: `t.polish.errorTryAgain` that calls `window.location.reload()`
       - Phone number fallback: `t.polish.errorCallUs` + `t.common.phone`
       - Dark/gold styling matching site aesthetic

    3. **Empty state** (fetch succeeded, zero vehicles, no error): Show a friendly empty state with:
       - Bilingual text: `t.polish.emptyInventory` (main message)
       - Subtext: `t.polish.emptyInventorySubtext`
       - CTA: Link to Contact page or phone number
       - Gold accent styling

    The key is checking BOTH `connectionError` (from Store) AND `sortedVehicles.length === 0` to distinguish between error and empty.

    **Home.tsx -- Featured vehicles section:**

    The Home page already has a `hasVehicles` check that hides the featured vehicles section entirely when there are no vehicles. This is acceptable behavior (hiding the section), but if you can improve it with a brief CTA like "Visit our lot to see what's available" that would be a nice touch. Use `t.polish.emptyInventory` or similar. Keep it subtle -- the Home page hero and other sections should still be the focus.

    **Also verify/add image lazy loading:**
    For any `<img>` tags in Inventory.tsx that render vehicle images BELOW the fold (in the grid), add `loading="lazy"` attribute. Do NOT add lazy loading to images that are above the fold or in the first visible viewport.

    **Important:**
    - Do NOT change the filter/sort UI or modal functionality
    - Do NOT change vehicle card design or layout (that's POLISH-08/09 scope)
    - All new text must use translation keys (t.polish.xxx)
    - Keep the existing loading spinner if it exists -- just add skeleton alternatives for the grid area
  </action>
  <verify>
    - Inventory.tsx has three distinct code paths: loading (skeleton), error (with retry), empty (with CTA)
    - Empty state uses t.polish.emptyInventory, error state uses t.polish.errorLoadFailed
    - Home.tsx featured vehicles section has some empty state handling
    - Vehicle grid images in Inventory.tsx have loading="lazy" where appropriate
    - No TypeScript errors: run build or type check
  </verify>
  <done>
    Inventory correctly distinguishes between loading, error, and empty states. Users never see a "Connection Issue" message when inventory is simply empty. Home handles zero-vehicle scenario gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Form error states + CustomerDashboard empty state + image lazy loading</name>
  <files>
    triple-j-auto-investment-main/pages/Contact.tsx
    triple-j-auto-investment-main/pages/Finance.tsx
    triple-j-auto-investment-main/pages/CustomerDashboard.tsx
    triple-j-auto-investment-main/pages/CustomerLogin.tsx
    triple-j-auto-investment-main/pages/About.tsx
    triple-j-auto-investment-main/pages/VinLookup.tsx
  </files>
  <action>
    **Contact.tsx -- Add error state to form submission:**

    Read the current Contact.tsx. It likely has form states like idle/sending/sent. Add an `error` state:
    - When the form submission (email service call) fails in the catch block, set state to 'error' instead of just logging to console
    - Render an error message when state is 'error': use `t.polish.errorFormSubmit`
    - Include a retry mechanism (button to reset to idle state so user can try again)
    - Include phone number fallback: `t.polish.errorCallUs` + `t.common.phone`
    - Style: red/amber warning styling within the dark aesthetic (bg-red-900/10 border border-red-500/30 or similar)

    **Finance.tsx -- Add error state to form submission:**

    Same pattern as Contact.tsx. Read the current Finance.tsx form submission logic. Add error state handling:
    - Catch block sets state to 'error'
    - Error UI with bilingual message, retry button, and phone fallback
    - Same styling pattern as Contact

    **CustomerDashboard.tsx -- Add empty state:**

    Read the current file. After data loads successfully but the customer has zero registrations, show an empty state:
    - Bilingual text: `t.polish.emptyDashboard` (main message)
    - Subtext: `t.polish.emptyDashboardSubtext`
    - Professional styling matching the existing page design
    - Do NOT change the loading state if one already exists

    **CustomerLogin.tsx -- Verify error handling:**

    Read the file and verify that OTP login errors show user-visible messages. If errors are only logged to console, add visible error text. The phone/code input flow should show errors like "Invalid code" or "Please try again" in the UI, not just console.

    **About.tsx -- Image lazy loading:**

    The About page loads external Unsplash background images. Add `loading="lazy"` to any `<img>` tags that are below the fold. If images are CSS background-image, they can't be lazy-loaded via HTML attribute -- note this but don't change the approach (CSS backgrounds are already deferred by the browser somewhat).

    Also add a loading state for the Google Maps iframe: wrap it in a container that shows a skeleton/placeholder until the iframe loads. Use `onLoad` event on the iframe.

    **VinLookup.tsx -- Verify states:**

    Read the file. It already has animated loading states for VIN lookups. Verify that error states from failed API calls show user-visible messages. If the NHTSA API fails, the user should see an error -- not just nothing. If already handled, no changes needed.

    **Important constraints:**
    - All new text through translation system (t.polish.xxx)
    - Do NOT restructure page layouts
    - Do NOT change successful submission flows (only add error handling)
    - Keep the dark/gold aesthetic for all new UI elements
  </action>
  <verify>
    - Contact.tsx has an error state that renders bilingual error UI with retry
    - Finance.tsx has an error state that renders bilingual error UI with retry
    - CustomerDashboard.tsx shows empty state when registrations array is empty
    - CustomerLogin.tsx shows visible error messages for OTP failures
    - About.tsx has loading="lazy" on below-fold images
    - VinLookup.tsx handles API errors with visible UI
    - Run build to confirm no TypeScript errors
  </verify>
  <done>
    All form submissions handle errors visually (not just console). CustomerDashboard has an empty state. All async operations across customer-facing pages show loading, success, and error states.
  </done>
</task>

</tasks>

<verification>
1. Inventory page: loading shows skeletons, error shows retry + phone, empty shows friendly message
2. Contact form: failed submission shows error UI with retry and phone fallback
3. Finance form: failed submission shows error UI with retry and phone fallback
4. CustomerDashboard: zero registrations shows empty state with explanatory text
5. All new text appears in both English and Spanish
6. Run `npm run build` -- zero errors
</verification>

<success_criteria>
- POLISH-05 (loading states): All async operations have visible loading indicators
- POLISH-06 (empty states): All data-dependent areas have zero-data fallbacks
- POLISH-07 (error states): Supabase failures and form errors show user-visible messages with recovery options
- All new UI text is bilingual via translation keys
</success_criteria>

<output>
After completion, create `.planning/phases/11-production-polish/11-02-SUMMARY.md`
</output>
