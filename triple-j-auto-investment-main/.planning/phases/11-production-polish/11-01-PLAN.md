---
phase: 11-production-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - triple-j-auto-investment-main/vite.config.ts
  - triple-j-auto-investment-main/App.tsx
  - triple-j-auto-investment-main/hooks/useOnlineStatus.ts
  - triple-j-auto-investment-main/components/OfflineBanner.tsx
  - triple-j-auto-investment-main/components/PageLoader.tsx
  - triple-j-auto-investment-main/components/ConnectionErrorBanner.tsx
  - triple-j-auto-investment-main/public/maintenance.html
  - triple-j-auto-investment-main/utils/translations.ts
autonomous: true

must_haves:
  truths:
    - "Production build produces zero console output in browser DevTools"
    - "When a lazy-loaded page is loading, the user sees a branded loading screen (not blank)"
    - "When a page component crashes, the user sees a helpful error screen with a reload button (not a blank screen)"
    - "When the browser goes offline, a visible banner appears telling the user"
    - "A branded maintenance page exists at /maintenance.html with Triple J logo, phone number, and dark/gold styling"
    - "All new UI text (offline banner, loading, error messages) works in both English and Spanish"
  artifacts:
    - path: "triple-j-auto-investment-main/vite.config.ts"
      provides: "Console stripping in production builds"
      contains: "esbuild"
    - path: "triple-j-auto-investment-main/hooks/useOnlineStatus.ts"
      provides: "Online/offline detection hook"
      exports: ["useOnlineStatus"]
    - path: "triple-j-auto-investment-main/components/OfflineBanner.tsx"
      provides: "Offline notification banner"
      exports: ["OfflineBanner"]
    - path: "triple-j-auto-investment-main/components/PageLoader.tsx"
      provides: "Branded Suspense fallback component"
      exports: ["PageLoader"]
    - path: "triple-j-auto-investment-main/components/ConnectionErrorBanner.tsx"
      provides: "Global Supabase connection error banner"
      exports: ["ConnectionErrorBanner"]
    - path: "triple-j-auto-investment-main/public/maintenance.html"
      provides: "Static branded maintenance page"
      min_lines: 30
    - path: "triple-j-auto-investment-main/utils/translations.ts"
      provides: "Bilingual keys for polish UI text"
      contains: "offline"
  key_links:
    - from: "triple-j-auto-investment-main/App.tsx"
      to: "triple-j-auto-investment-main/components/OfflineBanner.tsx"
      via: "import and render with useOnlineStatus"
      pattern: "useOnlineStatus|OfflineBanner"
    - from: "triple-j-auto-investment-main/App.tsx"
      to: "triple-j-auto-investment-main/components/PageLoader.tsx"
      via: "Suspense fallback prop"
      pattern: "Suspense fallback.*PageLoader"
    - from: "triple-j-auto-investment-main/App.tsx"
      to: "ErrorBoundary wrapping AppContent"
      via: "ErrorBoundary moved to wrap Routes/Suspense block"
      pattern: "ErrorBoundary"
---

<objective>
Build the production polish infrastructure: console stripping, error/loading/offline components, maintenance page, and all bilingual translation keys needed by subsequent plans.

Purpose: Subsequent plans (loading states, error states, mobile fixes) all depend on these shared components and translation keys existing. This plan creates the foundation.

Output: Vite config updated, 4 new components/hooks, maintenance page, and ~60 new translation keys in both English and Spanish.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-production-polish/11-CONTEXT.md
@.planning/phases/11-production-polish/11-RESEARCH.md

@triple-j-auto-investment-main/App.tsx
@triple-j-auto-investment-main/vite.config.ts
@triple-j-auto-investment-main/utils/translations.ts
@triple-j-auto-investment-main/context/Store.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Console stripping + ErrorBoundary fix + Suspense fallback + PageLoader component</name>
  <files>
    triple-j-auto-investment-main/vite.config.ts
    triple-j-auto-investment-main/App.tsx
    triple-j-auto-investment-main/components/PageLoader.tsx
  </files>
  <action>
    **1. Vite console stripping (vite.config.ts):**
    Add `esbuild.drop` to the build config to strip ALL console.* and debugger statements in production builds only. This satisfies POLISH-04 with a single config change.

    ```typescript
    build: {
      // existing rollupOptions...
      ...(mode === 'production' && {
        esbuild: {
          drop: ['console', 'debugger'],
        }
      })
    }
    ```

    Keep the `mode === 'production'` conditional so console statements still work during development.

    **2. PageLoader component (new file):**
    Create `components/PageLoader.tsx` -- a branded loading component used as Suspense fallback. Design:
    - Full viewport height, black background (matches site aesthetic)
    - Triple J gold logo (`/GoldTripleJLogo.png`) centered with `animate-pulse`
    - Small text below: "Loading..." in gold/gray, text-xs uppercase tracking-widest
    - Use the bilingual translation system -- import `useLanguage` and use `t.common.loading` for the text
    - Export as named export: `export const PageLoader`

    **3. Fix ErrorBoundary placement (App.tsx):**
    Currently the ErrorBoundary only wraps `<BrowserCompatibilityCheck />`. Move it to wrap the entire Routes/Suspense block inside AppContent. The ErrorBoundary should catch any page render crash.

    Change from:
    ```
    <ErrorBoundary><BrowserCompatibilityCheck /></ErrorBoundary>
    <Navbar />
    <main>...<Suspense>...</Suspense></main>
    ```

    To:
    ```
    <BrowserCompatibilityCheck />
    <Navbar />
    <ErrorBoundary>
      <main>...<Suspense>...</Suspense></main>
    </ErrorBoundary>
    ```

    Also update the ErrorBoundary's error UI: change `bg-tj-green` to `bg-black` (matches site aesthetic -- the site is dark themed, not green). Make the error message bilingual-aware if possible (the ErrorBoundary is a class component so it can't use hooks directly, but the reload button text and message can be hardcoded in English since error boundaries are edge cases).

    **4. Replace Suspense fallback (App.tsx):**
    Change `<Suspense fallback={null}>` to `<Suspense fallback={<PageLoader />}>`. Import PageLoader from the new component file.

    **Important:** Do NOT modify the SplashScreen, Navbar, Footer, or any route definitions. Only touch ErrorBoundary placement and Suspense fallback.
  </action>
  <verify>
    - Run `npm run build` (or the project's build command) in the triple-j-auto-investment-main directory and confirm it succeeds
    - Verify vite.config.ts contains `esbuild` with `drop: ['console', 'debugger']` inside the production conditional
    - Verify App.tsx has ErrorBoundary wrapping the main/Suspense block (not just BrowserCompatibilityCheck)
    - Verify Suspense fallback references PageLoader (not null)
    - Verify PageLoader.tsx exists and exports PageLoader component
  </verify>
  <done>
    Production build strips all console output. ErrorBoundary catches page crashes. Lazy-loaded pages show branded loading instead of blank screen.
  </done>
</task>

<task type="auto">
  <name>Task 2: Offline detection + connection error banner + maintenance page + translation keys</name>
  <files>
    triple-j-auto-investment-main/hooks/useOnlineStatus.ts
    triple-j-auto-investment-main/components/OfflineBanner.tsx
    triple-j-auto-investment-main/components/ConnectionErrorBanner.tsx
    triple-j-auto-investment-main/public/maintenance.html
    triple-j-auto-investment-main/utils/translations.ts
    triple-j-auto-investment-main/App.tsx
  </files>
  <action>
    **1. useOnlineStatus hook (new file):**
    Create `hooks/useOnlineStatus.ts` using the browser's `navigator.onLine` API and `online`/`offline` event listeners. Return a boolean `isOnline`. See 11-RESEARCH.md for the exact implementation pattern.

    **2. OfflineBanner component (new file):**
    Create `components/OfflineBanner.tsx`:
    - Fixed position at top of viewport, z-index very high (above navbar z-50, use z-[9999])
    - Red/dark background (bg-red-900/95 or similar), white text
    - Bilingual: use `useLanguage()` hook to get `t.polish.offlineBanner` text
    - Small, unobtrusive: text-xs uppercase tracking-widest
    - Includes "You appear to be offline" message and suggestion to check connection
    - Animates in/out smoothly (simple CSS transition or Framer Motion)

    **3. ConnectionErrorBanner component (new file):**
    Create `components/ConnectionErrorBanner.tsx`:
    - Reads `connectionError` from Store context (it already exists in Store.tsx but is never rendered)
    - Shows a banner when `connectionError` is truthy
    - Bilingual text: "Having trouble connecting to our servers. Some features may be unavailable."
    - Includes a retry button that calls `window.location.reload()` and a phone number link
    - Positioned below navbar (not fixed -- flows in document), styled with amber/warning colors (bg-amber-900/20 border border-amber-500/30)
    - Only renders when connectionError is truthy

    **4. Wire OfflineBanner and ConnectionErrorBanner into App.tsx:**
    In the AppContent component:
    - Import and use `useOnlineStatus` hook
    - Render `<OfflineBanner />` conditionally when `!isOnline` -- place it ABOVE the Navbar
    - Import and render `<ConnectionErrorBanner />` inside the main element, above the Routes (it will self-conditionally render based on Store's connectionError state)

    **5. Maintenance page (new static file):**
    Create `public/maintenance.html` -- a completely standalone static HTML file (no React, no build dependencies). Design:
    - Black background, centered content
    - Triple J logo: `<img src="/GoldTripleJLogo.png">`
    - Gold heading: "WE'LL BE BACK SHORTLY" / "REGRESAMOS PRONTO"
    - Gray body text: improvement messaging
    - Phone number link: (832) 400-9760
    - Business hours: Mon-Sat 9AM-6PM
    - Use inline CSS only (this must work without Tailwind/build pipeline)
    - Include both English and Spanish text (stacked, not toggled -- maintenance page should serve both audiences statically)
    - Professional, matching the dark/gold luxury aesthetic
    - See 11-RESEARCH.md for a template -- expand it with bilingual text

    **6. Add bilingual translation keys (translations.ts):**
    Add a new `polish` section to both `en` and `es` translation objects. These keys will be used by this plan and all subsequent plans in Phase 11. Add ALL of the following:

    ```
    polish: {
      // Offline
      offlineBanner: "You appear to be offline. Some features may be unavailable.",

      // Connection error
      connectionError: "Having trouble connecting. Some features may be unavailable.",
      connectionRetry: "Retry",
      connectionCallUs: "Call us at (832) 400-9760",

      // Loading states
      loadingInventory: "Loading inventory...",
      loadingPage: "Loading...",
      loadingSubmitting: "Submitting...",
      loadingSending: "Sending...",

      // Empty states
      emptyInventory: "No vehicles currently listed. Check back soon!",
      emptyInventorySubtext: "We regularly add new vehicles to our inventory.",
      emptyDashboard: "No registrations found.",
      emptyDashboardSubtext: "When you purchase a vehicle, your registration details will appear here.",

      // Error states
      errorGeneric: "Something went wrong. Please try again.",
      errorFormSubmit: "We couldn't submit your information. Please try again or call us.",
      errorLoadFailed: "Failed to load. Please check your connection and try again.",
      errorReload: "Reload Page",
      errorTryAgain: "Try Again",
      errorCallUs: "Or call us",

      // Accessibility
      skipToContent: "Skip to main content",
      closeModal: "Close",
      previousImage: "Previous image",
      nextImage: "Next image",
      openMenu: "Open menu",
      closeMenu: "Close menu",
      switchLanguage: "Switch language"
    }
    ```

    Add the Spanish equivalents for ALL keys. Use natural, conversational Spanish (not Google Translate):
    - offlineBanner: "Parece que no tienes conexion. Algunas funciones pueden no estar disponibles."
    - connectionError: "Problemas de conexion. Algunas funciones pueden no estar disponibles."
    - emptyInventory: "No hay vehiculos disponibles en este momento. Vuelve pronto!"
    - etc. (translate all keys with the same care given in Phase 10)

    **Important constraints:**
    - All new components must use the dark/gold aesthetic (bg-black, text-tj-gold, text-gray-400, etc.)
    - All user-facing text must come from the translation system (t.polish.xxx)
    - Do NOT modify the SplashScreen, route definitions, or Footer in this task
    - Do NOT add translation keys that already exist in translations.ts (check first -- t.common.loading already exists)
  </action>
  <verify>
    - Verify useOnlineStatus.ts exists in hooks/ and exports useOnlineStatus
    - Verify OfflineBanner.tsx exists and imports useLanguage + useOnlineStatus
    - Verify ConnectionErrorBanner.tsx exists and imports useStore
    - Verify App.tsx imports and renders OfflineBanner and ConnectionErrorBanner
    - Verify public/maintenance.html exists and contains both English and Spanish text, phone number, logo reference
    - Verify translations.ts contains the new `polish` section in both `en` and `es` objects
    - Run TypeScript compilation check to ensure no type errors
    - Run `npm run build` to confirm build succeeds
  </verify>
  <done>
    Offline detection works via browser API and shows a bilingual banner. Connection errors from Store are surfaced to users. A branded maintenance page exists for future use. All translation keys needed by Phase 11 plans are in place.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` -- should succeed with zero errors
2. Check that vite.config.ts has esbuild.drop for production
3. Check that App.tsx ErrorBoundary wraps Routes/Suspense (not just BrowserCompatibilityCheck)
4. Check that Suspense fallback is PageLoader (not null)
5. Check that OfflineBanner renders conditionally based on useOnlineStatus
6. Check that ConnectionErrorBanner reads connectionError from Store
7. Check that maintenance.html is standalone and bilingual
8. Check that translations.ts has polish section in both en and es
</verification>

<success_criteria>
- Zero console.* output in production build (POLISH-04 complete)
- ErrorBoundary catches page crashes and shows branded error UI
- Lazy page loads show branded loader instead of blank screen
- Offline state detected and communicated to user
- Connection error state surfaced to user (not just console.error)
- Maintenance page exists for future use
- All bilingual translation keys ready for subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/11-production-polish/11-01-SUMMARY.md`
</output>
