---
phase: 14-expectancy-building
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - triple-j-auto-investment-main/pages/VehicleDetail.tsx
  - triple-j-auto-investment-main/App.tsx
  - triple-j-auto-investment-main/pages/Inventory.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /vehicles/{slug} loads the correct vehicle and renders the detail page"
    - "Detail page shows hero gallery, identity headline, verified badge, price block, vehicle story, specs, social proof, and CTAs in psychological flow order"
    - "Detail page has unique SEO meta tags and Schema.org/Car structured data"
    - "Inventory grid cards link to the vehicle detail page via /vehicles/{slug}"
    - "Inventory grid cards show Triple J Verified badge on inspected vehicles"
    - "Inventory grid cards show identity headline (replacing or supplementing year/make/model)"
    - "Detail page is fully bilingual (en/es) via useLanguage"
    - "Detail page handles missing vehicle gracefully (404-like state)"
    - "Existing inventory modal continues to work unchanged for quick browsing"
  artifacts:
    - path: "triple-j-auto-investment-main/pages/VehicleDetail.tsx"
      provides: "Standalone vehicle detail page"
      min_lines: 150
    - path: "triple-j-auto-investment-main/App.tsx"
      provides: "Route registration for /vehicles/:slug"
      contains: "vehicles/:slug"
    - path: "triple-j-auto-investment-main/pages/Inventory.tsx"
      provides: "Minimal card updates (link, badge, headline)"
      contains: "VehicleVerifiedBadge"
  key_links:
    - from: "triple-j-auto-investment-main/App.tsx"
      to: "triple-j-auto-investment-main/pages/VehicleDetail.tsx"
      via: "Route element"
      pattern: "Route.*vehicles/:slug.*VehicleDetail"
    - from: "triple-j-auto-investment-main/pages/VehicleDetail.tsx"
      to: "triple-j-auto-investment-main/components/VehicleVerifiedBadge.tsx"
      via: "component import"
      pattern: "import.*VehicleVerifiedBadge"
    - from: "triple-j-auto-investment-main/pages/VehicleDetail.tsx"
      to: "triple-j-auto-investment-main/components/VehiclePriceBlock.tsx"
      via: "component import"
      pattern: "import.*VehiclePriceBlock"
    - from: "triple-j-auto-investment-main/pages/VehicleDetail.tsx"
      to: "triple-j-auto-investment-main/components/VehicleStorySection.tsx"
      via: "component import"
      pattern: "import.*VehicleStorySection"
    - from: "triple-j-auto-investment-main/pages/VehicleDetail.tsx"
      to: "triple-j-auto-investment-main/components/VehicleJsonLd.tsx"
      via: "component import"
      pattern: "import.*VehicleJsonLd"
    - from: "triple-j-auto-investment-main/pages/Inventory.tsx"
      to: "triple-j-auto-investment-main/pages/VehicleDetail.tsx"
      via: "Link to=/vehicles/{slug}"
      pattern: "Link.*vehicles/"
    - from: "triple-j-auto-investment-main/pages/VehicleDetail.tsx"
      to: "Supabase vehicles table"
      via: "direct query by slug or ID prefix"
      pattern: "supabase.*from.*vehicles"
---

<objective>
Create the vehicle detail page, register its route, and update inventory grid cards with minimal Phase 14 changes.

Purpose: This is the culminating plan of Phase 14 -- it assembles all the building blocks (types, services, components) from Plans 01-03 into the final vehicle detail page and connects it to the inventory grid. After this plan, visitors can navigate from inventory to a standalone vehicle page with identity headline, price transparency, verified badge, vehicle story, and SEO structured data.
Output: VehicleDetail.tsx page, /vehicles/:slug route in App.tsx, and minimal Inventory.tsx card updates (link to detail page, verified badge, identity headline).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-expectancy-building/14-CONTEXT.md
@.planning/phases/14-expectancy-building/14-RESEARCH.md
@.planning/phases/14-expectancy-building/14-01-SUMMARY.md
@.planning/phases/14-expectancy-building/14-02-SUMMARY.md
@.planning/phases/14-expectancy-building/14-03-SUMMARY.md

Key existing files:
@triple-j-auto-investment-main/App.tsx
@triple-j-auto-investment-main/pages/Inventory.tsx
@triple-j-auto-investment-main/components/ImageGallery.tsx
@triple-j-auto-investment-main/components/SEO.tsx
@triple-j-auto-investment-main/components/ScrollToTop.tsx
@triple-j-auto-investment-main/components/VehicleVerifiedBadge.tsx
@triple-j-auto-investment-main/components/VehiclePriceBlock.tsx
@triple-j-auto-investment-main/components/VehicleStorySection.tsx
@triple-j-auto-investment-main/components/VehicleJsonLd.tsx
@triple-j-auto-investment-main/types.ts
@triple-j-auto-investment-main/utils/vehicleSlug.ts
@triple-j-auto-investment-main/context/LanguageContext.tsx
@triple-j-auto-investment-main/supabase/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VehicleDetail.tsx page</name>
  <files>
    triple-j-auto-investment-main/pages/VehicleDetail.tsx
  </files>
  <action>
    Create NEW file: `pages/VehicleDetail.tsx`

    **Imports:**
    - React, useState, useEffect, useMemo from 'react'
    - useParams, Link from 'react-router-dom'
    - motion from 'framer-motion'
    - useLanguage from '../context/LanguageContext'
    - useStore from '../context/Store'
    - SEO from '../components/SEO'
    - ImageGallery from '../components/ImageGallery'
    - VehicleVerifiedBadge from '../components/VehicleVerifiedBadge'
    - VehiclePriceBlock from '../components/VehiclePriceBlock'
    - VehicleStorySection from '../components/VehicleStorySection'
    - VehicleJsonLd from '../components/VehicleJsonLd'
    - parseVehicleSlug from '../utils/vehicleSlug'
    - supabase from '../supabase/config'
    - Vehicle, VehicleStatus from '../types'
    - Lucide icons: ArrowLeft, Phone, Calendar, MapPin, Car, Gauge, Hash, Clock, Share2, ChevronLeft, ChevronRight, Loader2

    **Data fetching strategy:**
    Two-phase approach for fastest possible load:
    1. First, check if the vehicle exists in the store's vehicles array (already loaded from Supabase on app init). Match by comparing vehicle slug OR by short ID suffix from parseVehicleSlug against vehicle.id.startsWith(shortId).
    2. If not found in store (edge case: direct URL navigation before store loads), fetch directly from Supabase by slug column: `supabase.from('vehicles').select('*').eq('slug', slug).single()`
    3. If Supabase returns nothing, try fallback: parse shortId from slug and query `supabase.from('vehicles').select('*').ilike('id', shortId + '%').single()` (handles case where slug column hasn't been populated yet)
    4. Transform the Supabase response using the same snake_case-to-camelCase mapping as vehicles.ts (or import a shared transform helper if one exists)

    **State:**
    - `vehicle: Vehicle | null` -- the vehicle data
    - `isLoading: boolean` -- true while fetching
    - `notFound: boolean` -- true if vehicle not found
    - `galleryOpen: boolean` -- ImageGallery lightbox state
    - `galleryIndex: number` -- current image index in gallery

    **Page layout (psychological flow from research):**

    The page should be wrapped in a `<motion.div>` with fade-in animation. Max width container: max-w-5xl mx-auto.

    **Section 1: Back navigation**
    - "Back to Inventory" link (using t.vehicleDetail.backToInventory) with ArrowLeft icon
    - Links to /inventory
    - Sticky or fixed at top? No -- simple inline link at top

    **Section 2: Hero Image Gallery**
    - Large hero image: aspect-[16/9] on desktop, aspect-[4/3] on mobile
    - Show the primary image (vehicle.imageUrl) large
    - If vehicle has gallery images, show thumbnail strip below (max 4-5 visible, scroll for more)
    - Click on any image opens the existing ImageGallery component (fullscreen lightbox with swipe/zoom)
    - Image navigation: left/right arrows on hover (desktop), swipe (mobile via framer-motion drag)
    - Show image count badge: "{current} of {total}" from translations
    - If no images, show a placeholder with Car icon and "No photos available" text

    **Section 3: Identity Headline + Vehicle Info**
    - Identity headline (from vehicle.identityHeadline or identityHeadlineEs based on language) in font-display text-2xl md:text-3xl text-white
    - If no headline, fall back to "{year} {make} {model}"
    - Below headline: year, make, model in text-[10px] uppercase tracking-[0.2em] text-gray-400
    - Mileage with Gauge icon, status badge

    **Section 4: Triple J Verified Badge**
    - `<VehicleVerifiedBadge isVerified={vehicle.isVerified || false} size="lg" />`
    - Only renders if verified (component handles null return)

    **Section 5: Price Transparency Block**
    - `<VehiclePriceBlock price={vehicle.price} year={vehicle.year} mileage={vehicle.mileage} marketEstimate={vehicle.marketEstimate} />`

    **Section 6: Vehicle Story**
    - `<VehicleStorySection vehicle={vehicle} />`

    **Section 7: Vehicle Specs**
    - Grid of spec items (2 columns on mobile, 3 on desktop)
    - Each spec: icon + label + value
    - Specs to show: Year, Make, Model, Mileage, VIN (last 6 chars visible, rest masked for privacy), Status
    - Use icons from lucide-react: Calendar (year), Car (make/model), Gauge (mileage), Hash (VIN), etc.
    - Labels from translations: t.vehicleDetail.specs.*

    **Section 8: Social Proof Micro-Layer**
    - "Listed X days ago" -- calculate from vehicle.dateAdded
    - Use translations for "Listed today", "Listed yesterday", "Listed {days} days ago"
    - Small Clock icon + text in text-[10px] uppercase tracking-[0.2em] text-gray-400
    - Only show if dateAdded exists

    **Section 9: CTAs**
    - Two primary actions side by side:
      1. "Schedule a Visit" -- links to /contact (or could pre-fill with vehicle info via query param)
      2. "Call Us" -- tel:(832)400-9760
    - One secondary action:
      3. "Apply for Financing" -- links to /finance
    - Use canonical button pattern: py-4 px-8 text-xs tracking-[0.3em] uppercase
    - Primary (Schedule): bg-tj-gold text-black hover:bg-white
    - Secondary (Call): border border-tj-gold/30 text-tj-gold hover:bg-tj-gold/10
    - Tertiary (Finance): border border-white/[0.08] text-gray-400 hover:text-white

    **SEO:**
    - `<SEO title="{year} {make} {model} | Triple J Auto Investment" description="{identityHeadline or generated description}" path="/vehicles/{slug}" />`
    - `<VehicleJsonLd vehicle={vehicle} />`

    **Not Found State:**
    - If notFound is true, show centered message with Car icon, "Vehicle Not Found" heading, and link back to inventory
    - Use design system: bg-black, text-gray-400, font-display

    **Loading State:**
    - Centered Loader2 spinner with "Loading..." text
    - bg-black, min-h-screen

    **Share button:**
    - Small Share2 icon button near the top that copies the current URL to clipboard
    - Uses navigator.clipboard.writeText(window.location.href)
    - Shows brief "Copied!" feedback

    **Responsive design:**
    - Mobile-first: single column, full-width images
    - Desktop: max-w-5xl centered, sections with proper spacing
    - Section spacing: py-8 with border-t border-white/[0.04] between sections
    - Overall page padding: px-4 md:px-6
    - Page background: bg-black min-h-screen

    **Accessibility:**
    - All images have alt text: "{year} {make} {model} - photo {n} of {total}"
    - Keyboard navigable: all links and buttons focusable
    - Focus indicators: focus:ring-2 focus:ring-tj-gold/50 on interactive elements
  </action>
  <verify>
    Run `npx tsc --noEmit` from the triple-j-auto-investment-main directory. Zero type errors.
    Verify file exists and has substantial content: file should be 200+ lines.
    Verify all component imports: grep for VehicleVerifiedBadge, VehiclePriceBlock, VehicleStorySection, VehicleJsonLd, SEO, ImageGallery.
  </verify>
  <done>
    VehicleDetail.tsx renders a complete vehicle detail page with all 9 sections in psychological flow order. Data loads from store first, falls back to direct Supabase query. SEO meta tags and JSON-LD structured data included. Bilingual via useLanguage. Handles loading, not-found, and missing data gracefully. Follows design system throughout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register /vehicles/:slug route in App.tsx</name>
  <files>
    triple-j-auto-investment-main/App.tsx
  </files>
  <action>
    **Add lazy import** for VehicleDetail at the top of App.tsx, alongside the other lazy-loaded pages. Use the existing lazyWithErrorHandling pattern:

    ```typescript
    const VehicleDetail = lazyWithErrorHandling(() => import('./pages/VehicleDetail'), 'Vehicle Detail');
    ```

    Place this with the other lazy imports (NOT with the eagerly loaded Home/Inventory/Contact).

    **Add route** in the Routes block. Place it right after the `/inventory` route (line ~592):

    ```typescript
    <Route path="/vehicles/:slug" element={<VehicleDetail />} />
    ```

    This must be BEFORE the `<Route path="*" element={<NotFound />} />` catch-all.

    **No other changes to App.tsx.** The existing ScrollToTop component, AnimatePresence wrapper, and Suspense fallback all work automatically for this new route.
  </action>
  <verify>
    Run `npx tsc --noEmit` from the triple-j-auto-investment-main directory. Zero type errors.
    Verify route exists: `grep "vehicles/:slug" App.tsx` returns a match.
    Verify lazy import exists: `grep "VehicleDetail" App.tsx` returns at least 2 matches (import + route).
    Run `npx vite build` from the triple-j-auto-investment-main directory. Build succeeds.
  </verify>
  <done>
    /vehicles/:slug route registered in App.tsx. VehicleDetail lazy-loaded with error handling. Route placed before catch-all. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Minimal inventory grid card updates (link, badge, headline)</name>
  <files>
    triple-j-auto-investment-main/pages/Inventory.tsx
  </files>
  <action>
    Make THREE minimal changes to the VehicleCard component in Inventory.tsx. Per the user's decision, inventory grid changes are CONSERVATIVE -- no layout redesign.

    **Import additions:**
    - `import { Link } from 'react-router-dom';` (already imported at module level in Inventory.tsx if not in VehicleCard scope -- verify)
    - `import { VehicleVerifiedBadge } from '../components/VehicleVerifiedBadge';`
    - `import { generateVehicleSlug } from '../utils/vehicleSlug';`

    **Change 1: Add Verified Badge to card**
    In the VehicleCard component, inside the badges row (the `<div className="absolute top-0 left-0 right-0 z-20 p-4 flex justify-between items-start pointer-events-none">` around line 74), add the VehicleVerifiedBadge AFTER the existing status badges:

    ```tsx
    {vehicle.isVerified && (
      <VehicleVerifiedBadge isVerified={true} size="sm" />
    )}
    ```

    Place it as the last child of the badges row. If there's already a right-aligned badge (the "SALE & RENTAL" badge), position the verified badge below it or adjust the flex layout to accommodate both. Use `pointer-events-auto` on the badge wrapper so it can show tooltip on hover.

    **Change 2: Show identity headline on card**
    In the content section of VehicleCard (around line 150-158), where it currently shows:
    ```
    <p className="text-[9px]...">{vehicle.year} {vehicle.make}</p>
    <h3 className="font-display text-xl...">{vehicle.model}</h3>
    ```

    Add the identity headline ABOVE the year/make line if it exists:
    ```tsx
    {(lang === 'es' ? vehicle.identityHeadlineEs : vehicle.identityHeadline) && (
      <p className="text-[8px] uppercase tracking-[0.15em] text-tj-gold/70 mb-1 truncate">
        {lang === 'es' ? vehicle.identityHeadlineEs : vehicle.identityHeadline}
      </p>
    )}
    <p className="text-[9px] uppercase tracking-[0.2em] text-gray-400 mb-1 group-hover:text-tj-gold/60 transition-colors">{vehicle.year} {vehicle.make}</p>
    <h3 className="font-display text-xl md:text-2xl text-white group-hover:text-tj-gold transition-colors leading-none tracking-tight truncate">
      {vehicle.model}
    </h3>
    ```

    The headline is a SUPPLEMENT to, not a replacement for, the year/make/model. Keep it subtle (text-[8px], gold/70 opacity, truncated).

    Access `lang` from useLanguage -- the VehicleCard already has access to `t` via useLanguage, so add `lang` to the destructured return if not already there.

    **Change 3: Link card to detail page**
    Wrap the VehicleCard's main onClick to also function as a link to the detail page. There are two approaches -- choose the simpler one:

    Option A (preferred): Add a small "View Details" link/button that navigates to `/vehicles/{slug}`. Place it next to the existing "Express Interest" and "Book Now" buttons in the CTA row (line 176-191). This is minimally invasive:

    ```tsx
    <Link
      to={`/vehicles/${vehicle.slug || generateVehicleSlug(vehicle.year, vehicle.make, vehicle.model, vehicle.id)}`}
      onClick={(e) => e.stopPropagation()}
      className="group/btn px-3 py-2.5 min-h-[44px] text-[9px] font-bold uppercase tracking-[0.2em] transition-all active:scale-95 bg-white/[0.04] text-gray-400 border border-white/[0.08] hover:bg-white/10 hover:text-white hover:border-white/20 flex items-center gap-1"
    >
      {t.vehicleDetail.viewDetails || 'Details'}
      <ArrowUpRight size={10} />
    </Link>
    ```

    Add `viewDetails: "View Details"` / `viewDetails: "Ver Detalles"` to the vehicleDetail translations if not already present (or use a common translation key).

    The existing card onClick still opens the modal for quick browsing. The new Link button navigates to the standalone detail page. Both coexist.

    **DO NOT change** the modal behavior, card layout, filtering, sorting, or any other existing Inventory.tsx functionality. Only these three additions.
  </action>
  <verify>
    Run `npx tsc --noEmit` from the triple-j-auto-investment-main directory. Zero type errors.
    Run `npx vite build` from the triple-j-auto-investment-main directory. Build succeeds.
    Verify VehicleVerifiedBadge import: `grep "VehicleVerifiedBadge" pages/Inventory.tsx` returns a match.
    Verify link to detail page: `grep "vehicles/" pages/Inventory.tsx` returns a match.
    Verify identity headline render: `grep "identityHeadline" pages/Inventory.tsx` returns a match.
  </verify>
  <done>
    Inventory grid cards show Triple J Verified badge on inspected vehicles. Identity headline appears above year/make/model as a subtle gold supplement. A "View Details" link navigates to /vehicles/{slug} detail page. Existing modal and card behavior unchanged. Build passes.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx vite build` succeeds
- VehicleDetail.tsx exists with 200+ lines
- /vehicles/:slug route registered in App.tsx
- Inventory cards show verified badge, identity headline, and detail page link
- Existing modal continues to work
- Detail page renders all 9 sections in order
- Detail page has SEO meta tags and JSON-LD
- Detail page is bilingual
- Detail page handles loading, not-found, and missing data
- All buttons/links meet 44px touch target minimum
- Text contrast meets WCAG AA (text-gray-400 minimum on black)
</verification>

<success_criteria>
A visitor can browse the inventory grid (which now shows verified badges and identity headlines), click "View Details" to navigate to /vehicles/{slug}, and see a complete vehicle detail page with hero gallery, identity headline, verified badge, price transparency block, vehicle story, specs, social proof, and CTAs. The page is bilingual, SEO-optimized, and follows the established design system. The existing modal continues to work for quick browsing.
</success_criteria>

<output>
After completion, create `.planning/phases/14-expectancy-building/14-04-SUMMARY.md`
</output>
