---
phase: 17-divine-response
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - triple-j-auto-investment-main/pages/VehicleDetail.tsx
  - triple-j-auto-investment-main/pages/Inventory.tsx
  - triple-j-auto-investment-main/services/retellService.ts
  - triple-j-auto-investment-main/services/trackingService.ts
autonomous: true

must_haves:
  truths:
    - "Chat widget appears on vehicle detail pages with context about the specific vehicle being viewed"
    - "Chat widget appears on the inventory page (without vehicle-specific context, general dealership chat)"
    - "Retell outbound calls pass rental-specific context (is_rental, daily_rate, weekly_rate) when inquiry is for a rental"
    - "When Retell credentials are missing, the fallback shows a human-friendly phone number message"
    - "Chat interactions are tracked as behavioral events (chat_open, chat_message)"
  artifacts:
    - path: "triple-j-auto-investment-main/pages/VehicleDetail.tsx"
      provides: "ChatWidget integrated into vehicle detail page with full vehicle context"
      contains: "ChatWidget"
    - path: "triple-j-auto-investment-main/pages/Inventory.tsx"
      provides: "ChatWidget available on inventory listing page"
      contains: "ChatWidget"
    - path: "triple-j-auto-investment-main/services/retellService.ts"
      provides: "Updated Retell service with rental inquiry context, Spanish language note, improved fallback"
      contains: "is_rental"
    - path: "triple-j-auto-investment-main/services/trackingService.ts"
      provides: "New tracking event types for chat interactions"
      contains: "chat_open"
  key_links:
    - from: "VehicleDetail.tsx"
      to: "ChatWidget"
      via: "component import with vehicle prop"
      pattern: "<ChatWidget.*vehicle="
    - from: "Inventory.tsx"
      to: "ChatWidget"
      via: "component import"
      pattern: "<ChatWidget"
    - from: "retellService.ts"
      to: "Retell API"
      via: "dynamic variables with rental context"
      pattern: "is_rental|daily_rate|weekly_rate"
    - from: "VehicleDetail.tsx"
      to: "trackingService"
      via: "trackEvent for chat_open"
      pattern: "trackEvent.*chat_open"
---

<objective>
Wire the chat widget into vehicle pages, update Retell for rental inquiries and Spanish support, and add chat tracking events.

Purpose: This plan connects all the pieces -- the chat UI (Plan 02) gets integrated into the actual pages where visitors browse, Retell gets rental + bilingual context for voice calls, and chat interactions feed into the behavioral intelligence layer (Phase 16).

Output: ChatWidget on VehicleDetail and Inventory pages, updated retellService.ts, chat tracking events
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-divine-response/17-RESEARCH.md
@.planning/phases/17-divine-response/17-01-SUMMARY.md
@.planning/phases/17-divine-response/17-02-SUMMARY.md

# Files to modify
@triple-j-auto-investment-main/pages/VehicleDetail.tsx
@triple-j-auto-investment-main/pages/Inventory.tsx
@triple-j-auto-investment-main/services/retellService.ts
@triple-j-auto-investment-main/services/trackingService.ts

# Dependencies from Plans 01+02
@triple-j-auto-investment-main/components/chat/ChatWidget.tsx
@triple-j-auto-investment-main/hooks/useDivineChat.ts
@triple-j-auto-investment-main/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate ChatWidget into VehicleDetail and Inventory pages</name>
  <files>
    triple-j-auto-investment-main/pages/VehicleDetail.tsx
    triple-j-auto-investment-main/pages/Inventory.tsx
    triple-j-auto-investment-main/services/trackingService.ts
  </files>
  <action>
**trackingService.ts** -- Extend tracking event types:

1. Find the `TrackingEventType` definition in types.ts. Add `'chat_open' | 'chat_message'` to the union type.
   - Actually, check: if TrackingEventType is in types.ts (it is -- line 91), add the new types there instead. Update the type union to include `'chat_open' | 'chat_message'`.
   - `chat_open`: fired when user opens the chat widget
   - `chat_message`: fired when user sends a message (metadata includes messageCount and identifiedProfile)

**VehicleDetail.tsx** -- Add ChatWidget:

1. Add import: `import { ChatWidget } from '../components/chat/ChatWidget'`
2. Find the return JSX of the VehicleDetail component. Add `<ChatWidget vehicle={vehicle} />` AFTER the closing of the main content wrapper (but inside the parent fragment/div). The ChatWidget is position:fixed so it doesn't affect layout -- it just needs to be rendered somewhere in the component tree.
3. The vehicle object is already available in VehicleDetail (it's the main data object the page renders). Pass it directly as prop.
4. Add a `trackEvent` call for `chat_open` -- BUT this should happen inside ChatWidget itself (when isOpen toggles to true). Since ChatWidget is a generic component, it's better to add the tracking there. However, to avoid modifying Plan 02 files, add a simple wrapper approach:
   - Actually, the cleanest approach: In VehicleDetail.tsx, do NOT add tracking here. Instead, update the ChatWidget component to accept an optional `onOpen` callback prop, and pass `() => trackEvent({ event_type: 'chat_open', vehicle_id: vehicle.id, ... })` from VehicleDetail. BUT that modifies ChatWidget (Plan 02 file).
   - Simpler approach that avoids touching Plan 02 files: Add the tracking directly in useDivineChat hook via an exported `trackChatOpen` function in trackingService. Since useDivineChat is also Plan 02...
   - Best approach: Just add `chat_open` and `chat_message` tracking inside ChatWidget.tsx. Since ChatWidget was created in Plan 02 and this plan depends on 02, we CAN modify it. Add:
     - When `isOpen` changes from false to true: call `trackEvent({ session_id: getSessionId(), event_type: 'chat_open' as TrackingEventType, vehicle_id: vehicle.id, page_path: window.location.pathname, metadata: {}, created_at: new Date().toISOString() })`
     - When sendMessage is called in useDivineChat: track `chat_message` with metadata `{ messageCount: messages.length, profile }`. Add this inside the sendMessage function in useDivineChat.ts.
   - Since both ChatWidget.tsx and useDivineChat.ts are listed in Plan 02 as files_modified, and this plan depends on Plan 02, we should add these tracking calls. Update the imports in ChatWidget.tsx to include trackEvent and getSessionId from trackingService. Add the tracking at the points described above.

5. Important: Do NOT move or re-order existing sections in VehicleDetail.tsx. The ChatWidget is a fixed-position overlay, so just add it at the end of the JSX.

**Inventory.tsx** -- Add ChatWidget for general dealership chat:

1. For the inventory page, the chat should work WITHOUT a specific vehicle context (visitor is browsing, not on a detail page).
2. Create a "general" vehicle context object for Inventory chat:
   ```
   const generalChatVehicle: Vehicle = {
     id: 'general',
     make: 'Triple J',
     model: 'Auto Investment',
     year: new Date().getFullYear(),
     price: 0,
     mileage: 0,
     vin: '',
     status: VehicleStatus.AVAILABLE,
     description: 'Browse our inventory of pre-owned vehicles in the $3,000-$8,000 range.',
     imageUrl: '',
   };
   ```
   This way the ChatWidget can render, and the Edge Function system prompt will show general dealership info (since price/mileage are 0, the prompt focuses on dealership knowledge).
3. Add `import { ChatWidget } from '../components/chat/ChatWidget'` and render `<ChatWidget vehicle={generalChatVehicle} />` at the end of the Inventory JSX return, outside the main content flow.
4. This gives visitors on the inventory browse page access to chat for general questions about the dealership, financing, etc.
  </action>
  <verify>
    - VehicleDetail.tsx imports and renders ChatWidget with the vehicle prop
    - Inventory.tsx imports and renders ChatWidget with general dealership context
    - types.ts TrackingEventType includes 'chat_open' and 'chat_message'
    - ChatWidget.tsx has trackEvent calls for chat_open (on panel open) and useDivineChat.ts has chat_message tracking (on send)
    - `npm run build` passes
    - No layout shifts or z-index conflicts (ChatWidget is fixed position, z-9998)
  </verify>
  <done>
    - Chat widget visible and functional on vehicle detail pages with full vehicle context
    - Chat widget visible on inventory page with general dealership context
    - Chat opens and messages tracked as behavioral events
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Retell service for rental inquiries and fallback</name>
  <files>
    triple-j-auto-investment-main/services/retellService.ts
  </files>
  <action>
Update `retellService.ts` with three changes:

**1. Add rental inquiry context to VehicleInquiryPayload and dynamic variables:**

Add optional fields to VehicleInquiryPayload interface:
```typescript
daily_rate?: string;
weekly_rate?: string;
```

In `triggerOutboundCall`, detect rental inquiries and add context:
```typescript
const isRentalInquiry = payload.inquiry_source === 'rental_inquiry' ||
                        payload.inquiry_source === 'rental';

// Add to retell_llm_dynamic_variables:
is_rental: isRentalInquiry ? 'yes' : 'no',
daily_rate: payload.daily_rate || '',
weekly_rate: payload.weekly_rate || '',
```

This passes `{{is_rental}}`, `{{daily_rate}}`, `{{weekly_rate}}` to the Retell agent prompt so it can adapt the conversation for rental vs purchase inquiries.

**2. Add Spanish language note to metadata:**

Add a `preferred_language` field to the metadata object in the API call body:
```typescript
metadata: {
  vehicle_id: payload.vehicle_id,
  inquiry_timestamp: payload.inquiry_timestamp,
  source: 'website_inventory',
  preferred_language: payload.preferred_language || 'en',
}
```

Add `preferred_language?: string` to VehicleInquiryPayload. When the visitor is browsing in Spanish (lang='es'), the calling code can pass `preferred_language: 'es'` so the Retell agent knows to greet in Spanish.

Note: Actual Retell agent Spanish language configuration must be done in the Retell dashboard (set language to 'es' or enable multilingual mode). The code just passes the context -- the dashboard config is user_setup.

**3. Improve error fallback message:**

Update the error return in the credential check to be more specific:
```typescript
return {
  success: false,
  error: 'Voice assistant is currently unavailable. Please call us directly at (832) 400-9760 -- we are happy to help!'
};
```

Also update the catch block's generic error to include the phone number:
```typescript
return {
  success: false,
  error: `Voice call could not be placed. Please call us at (832) 400-9760. ${error instanceof Error ? `(${error.message})` : ''}`
};
```

This ensures DIVINE-07 (graceful fallback) is met -- when Retell is down, the user always sees the phone number.
  </action>
  <verify>
    - retellService.ts VehicleInquiryPayload has daily_rate, weekly_rate, preferred_language fields
    - triggerOutboundCall passes is_rental, daily_rate, weekly_rate in dynamic variables
    - metadata includes preferred_language
    - Both error paths (credential check + API failure) include (832) 400-9760 phone number
    - `npm run build` passes (no TS errors)
    - Existing triggerOutboundCall callers still work (new fields are optional)
  </verify>
  <done>
    - Retell voice agent receives rental context (is_rental flag, rates) for rental inquiries
    - Language preference passed through metadata for Spanish support
    - All error paths show phone number as fallback
    - Existing callers unaffected (backward compatible)
  </done>
</task>

</tasks>

<verification>
1. ChatWidget renders on VehicleDetail with specific vehicle data
2. ChatWidget renders on Inventory with general dealership context
3. Chat open and message events tracked in session_events
4. Retell passes rental context and language preference
5. All Retell error paths include phone number fallback
6. Full build passes: `npm run build`
7. No z-index conflicts between chat widget and existing UI (navbar, mobile menu, modals)
</verification>

<success_criteria>
- Chat widget accessible on both vehicle detail and inventory pages
- Vehicle detail chat knows the specific vehicle being viewed
- Retell updated for rental inquiries with is_rental, daily_rate, weekly_rate dynamic vars
- Retell errors always show (832) 400-9760 phone number
- Chat interactions tracked as behavioral events
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-divine-response/17-03-SUMMARY.md`
</output>
